# Streamline Ledger — Area: rust-quality

Synthesized from 8 unit agents covering `crates/quality/**` plus `crates/server` gapfill.
After dedup and cross-link: **26 candidates**. Ranked by removal priority (high-confidence + zero-blast-radius deletes first).

> **fast-context UNAVAILABLE incident:** Every unit reported `mcp__fast-context__fast_context_search` returning `resource_exhausted` (quota/backend), not auth. All cross-file caller/callee analysis fell back to repo-wide Grep. Trace IDs collected: `39cd8a2eb14044cd886d8a00fe5ebb5f`, `8841d74df6474cb28b35951af4a6dbab`, `02223e87eb3d4847bff5baa5f66bbc7e`, `f9565ccde2834c30be2609e5d2ee6b64`, `4c74acd196e149b7a039ce4256183e3c`, `68ee1ea2ca2a447d9d42edd7a029b296`, `fe311bf1328940c8956287f3802210be`, `87582278c5024143b89d602fd41fffd0`, `740e6caff2e1406c81910efb394bc997`, `8c43b6fa749c4104909a515f279ac416`. Consequence: trait-dispatch / macro / reflection callers were not semantically re-verified. Plain struct/enum/fn dead-code claims are well-grounded (exhaustive Grep); public-enum/serde-API claims are treated conservatively.

## Synthesis judge corrections (verified this pass)
- **RQ-TS-01 corrected**: unit `rs-quality-rules-ts` claimed TypeScript `TodoCommentsRule` is missing from `all_ts_rules()`. **False** — `crates/quality/src/rules/typescript/mod.rs:64` registers `Box::new(todo_comments::TodoCommentsRule)`. The TS rule is LIVE. Candidate reclassified from "dead orphan" to "no action / not dead".
- Verified zero-caller deletes by Grep: `QualityGate::with_id`, `Operator::to_db_value`, `QualityIssue::as_legacy`, `import_sarif_results` (def-only) — all confirmed.

## Ledger

| id | path(s):lines | kind | disposition | conf | evidence | blastRadius | cross-links |
|----|----------------|------|-------------|------|----------|-------------|-------------|
| RQ-01 | gate/mod.rs:46-56 | dead | delete | high | `QualityGate::with_id` 0 callers (Grep: only def; other `with_id` hits are tray `MenuItem`). Prod builds gates via `QualityGate::new` (config.rs:327). | None — no callers, no tests. | — |
| RQ-02 | gate/condition.rs:36-41 | dead | delete | high | `Operator::to_db_value` 0 callers. Only inverse `from_db_value` used (config.rs:78); persistence via serde rename. | None. | RQ-03 (same file cluster) |
| RQ-03 | gate/condition.rs:124-126 | dead | delete | high | `Condition::description` 0 callers; evaluator builds messages inline. | None. | RQ-02, RQ-04 |
| RQ-04 | server/self_test/runner.rs:19,131 | dead | delete | high | `TestServer::port` set from `local_addr().port()` never read; WS test recovers port by splitting `base_url`. `server.port` 0 matches outside runner.rs. | Remove field+assignment; no other file touches. | — (gapfill) |
| RQ-05 | rust/error_handling.rs:47,64-66 | bug | refactor | high | `in_test` init false, never set true → guards (L112/120/135/148) always take attr branch; dead conditional. `content:&str` (#[allow(dead_code)]) never read. | Local; removing `content` shrinks lifetime + drops allow. | — |
| RQ-06 | provider/frontend.rs:708-713 | bug | refactor | high | `parse_eslint_summary` compiles `Regex::new` per call in hot analyze() loop; rest of file uses OnceLock. | Trivial; wrap in OnceLock<Regex>. | RQ-07, RQ-08 (regex-recompile cluster) |
| RQ-07 | provider/sonar.rs:150-181 | bug | refactor | high | `wait_for_quality_gate(_task_id:&str)` param unused; call site (L285) passes `""`. Polls latest scan, races concurrent scans. API misleading. | Self-contained (1 caller). | RQ-13, RQ-14 (sonar cluster) |
| RQ-08 | weak_default_detection.rs:103-165 | bug | refactor | high | Provider `files_for_scope` only feeds .rs/.ts/.js/.tsx/.jsx; rule early-returns unless .yml/.yaml/.env/.json/.toml/.js/.ts — so Docker-Compose + .env + Blocker JWT-secret patterns NEVER fire. Only JS/TS `||'dev_secret'` path is live. | Expand provider filter to feed infra files (activates) OR drop dead arms. Changes provider behavior. No gate currently reads it (folded into builtin_common_issues GT 5). | RQ-09 (same security feature) |
| RQ-09 | provider/builtin_common.rs:137-145 | dubious-feature | refactor | medium | Blocker-severity weak-default issues only counted in `BuiltinCommonIssues` aggregate (repo gate GT 5), no dedicated MetricKey / zero-tolerance condition like `secrets_detected`. Up to 5 can pass silently. | Additive: add `MetricKey::WeakDefaultsDetected` + GT 0 condition. No downstream reader. | RQ-08 |
| RQ-10 | coverage_parser.rs:168-180 | bug | refactor | medium | `extract_attr_f64/_u64` call `Regex::new` per invocation; `parse_cobertura` compiles 6 regexes/parse. No cache. | Contained; move to LazyLock or str::find. | RQ-06, RQ-08 (regex-recompile cluster) |
| RQ-11 | rust/type_complexity.rs:202-205 | bug | refactor | medium | `let _ = init;` bind-and-discard inside `if let Some(ref init)`; comment claims init used but actual descent is `syn::visit::visit_local` (L212). Dead statement. | Zero output change. | — |
| RQ-12 | discovery/mod.rs:624-656 | dubious-feature | refactor | medium | `is_stub_script_body`/`pick_real_script` (R4 anti-stub gate) use fixed TOOL_TOKENS allowlist missing oxlint/deno/bun/cargo-nextest → unlisted-tool scripts misclassified as stubs. Brittle, NOT dead. | Refactor (expand/invert allowlist), don't delete — deletion re-exposes R4 bypass. | — (security-critical, see Invisible) |
| RQ-13 | provider/sonar.rs:62-100 | dead | investigate | medium | `import_sarif_results` pub, 0 callers (Grep confirms def-only). analyze() path calls sonar-scanner CLI directly. Orphaned design artifact. | Zero runtime; removing only breaks unseen external API consumers (none found). | RQ-07, RQ-14 |
| RQ-14 | provider/sonar.rs:1-297 | dubious-feature | investigate | low | SonarProvider needs running SonarQube + sonar-scanner CLI + SONAR_TOKEN; fails closed in normal CI. Gated by `config.providers.sonar` (engine.rs L101), assumed default false (unverified in config.rs). | If deleted: drop engine.rs L101-110 + config field. Affects users who enabled sonar. | RQ-07, RQ-13 |
| RQ-15 | provider/delivery_readiness.rs:223-348 | legacy | investigate | medium | `detect_wrong_package_load_test_coverage`, `detect_duplicate_load_testing_implementation`, `detect_i18n_namespace_mismatch` hardcode Hoppscotch paths (packages/hoppscotch-common/-app); SoloDawn has none → always no-op. | Remove 3 private fns + call sites; their metrics still fed by other detectors. | — |
| RQ-16 | gate/result.rs:101-113 | dead | investigate | medium | `EvaluationResult::warn` 0 callers; sole producer of `Level::Warn` (read at mod.rs:63). Warn aggregation branch never exercised. Shadow/warn done at orchestrator-mode level instead (parallel mechanism). | Deleting makes Level::Warn / Status::Warn unreachable; agent.rs:2829/7936 + report.rs:116 Warn arms become dead-but-compile. Confirm streamline isn't folding shadow into gate model first. | RQ-17, RQ-18 (gate-Warn cluster) |
| RQ-17 | gate/condition.rs:97-121 | redundant | investigate | medium | `parse_threshold_f64/_i64` no prod callers; only own tests (condition.rs:169-170). Evaluator uses private `parse_threshold` (evaluator.rs:135-150). | Delete with `test_threshold_parsing`. Check no future config-validation path intended. | RQ-02, RQ-03 |
| RQ-18 | metrics.rs:55-68 | dead | investigate | medium | MetricKey `Bugs/NewBugs/CodeSmells/Vulnerabilities/DuplicatedLinesDensity` never emitted by any provider, never in any gate condition (3 yaml + build.rs FALLBACK + default_config); appear only in condition.rs/evaluator.rs tests. | Public enum + serde rename. Removing touches exhaustive as_str/display_name arms; breaks deserialization of external yaml using those keys. Keep if Sonar fwd-compat intended. | RQ-16, RQ-21 |
| RQ-19 | build.rs:21-116 | redundant | refactor | medium | `FALLBACK_POLICY` is a hand-maintained 2nd copy of enforce policy that drifts from quality-gate.yaml: includes `eslint_errors` in branch/repo gates (L67,89) which yaml deliberately omits (advisory), omits completeness/delivery_readiness. Used only out-of-tree with no resolvable central yaml. | Out-of-tree builds only. Trim to compile-only stub or add build-time consistency assert. Do NOT delete (hard contract: crate must compile anywhere — primary-brain rejection v1). | RQ-20 (policy-duplication cluster) |
| RQ-20 | config.rs:219-311 | redundant | investigate | low | `default_config()` reachable only if BUNDLED_CENTRAL_POLICY fails to parse (L208-215), but test (L368-380) asserts it always parses + build.rs guarantees valid embed → unreachable in prod. 3rd policy def (lenient Shadow) that can drift. | Used by 3 unit tests + last-resort fallback. Keep crash-safety but consider fail-closed enforce stub vs lenient Shadow; touches config tests. | RQ-19 |
| RQ-21 | gate/result.rs:18-19 | dead | keep | low | `MeasureValue::None` variant never constructed (only Display arm result.rs:69). Absence expressed via `Option<MeasureValue>=None`. | Serde enum persisted in DB JSON (quality_run.rs decision blob). Removing could break deserializing historical rows. Keep pending migration audit. | RQ-18 |
| RQ-22 | gate/mod.rs:100-110 | dead | investigate | low | `is_blocked`/`failed_conditions` called only by own tests (mod.rs:184-185); prod uses `is_passed()`+status string. | Low — `is_passed()` IS used; these are cheap idiomatic accessors maybe wanted by reporting UI. Keep-or-defer. | RQ-01 |
| RQ-23 | issue.rs:147-150 | dead | investigate | medium | `QualityIssue::as_legacy` 0 callers outside issue.rs (Grep). Sets is_new=false; new-vs-legacy distinction unused — nothing marks an issue legacy, is_new stays true. | pub on crate consumed by services; downstream caller possible. Only affects builder API. | RQ-24 |
| RQ-24 | issue.rs:227-241,163-170 | dead | investigate | low | `IssueSummary::one_line_summary` + `QualityIssue::location_string` 0 callers (to_fix_instruction IS used via report→agent.rs; these two are not). Leftover formatters. | Public API of shared crate; could be out-of-tree consumer or future logging. Low internal risk. | RQ-23 |
| RQ-25 | server/self_test/tests.rs:29,62 | dead | investigate | medium | `TestContext::org_id` declared + init None; no test assigns or reads it. Scaffolded for never-written org-scoped tests. | Remove field + None init; no reader. May be harmless scaffolding if orgs group stays read-only. | — (gapfill) |
| RQ-26 | server/benches/performance.rs (whole) | dubious-feature | investigate | medium | Self-documented disclaimers W2-05-01..08: no real SQLite/HTTP, Vec<u8> allocs not queries, no CI baseline. bench_db_queries measures allocator; bench_async_tasks measures tokio bookkeeping. No actionable signal. | Whole file; no prod dep. Confirm CI-gating status before delete. | — (gapfill) |

### Not-dead / corrected (no removal action)
| id | path:lines | reason |
|----|------------|--------|
| RQ-TS-01 | typescript/todo_comments.rs | **Corrected**: IS registered at mod.rs:64. Live rule, not orphaned. No action. |
| RQ-KEEP-01 | engine.rs:50-70 | `QualityScope` merge_base/head: written by agent.rs (real git), read-discarded in-crate (V1 compat shim, phase-2 provider-native scope reserved). Removing breaks agent.rs construction. KEEP, flag incomplete feature. |
| RQ-KEEP-02 | discovery/mod.rs:215-218 | `RepoChecks.prepare_db`: populated + consumed in provider/repo.rs:63-95. Not dead. KEEP. |
| RQ-DUP-01 | any_usage.rs:51-78 vs console_usage.rs is_comment_line | Two `is_comment_line` (stateful vs stateless). Consolidation candidate (refactor, low priority); stateless version misses block-comment continuation (potential FP, untested). |
| RQ-DUP-02 | complexity.rs:155-173 + function_length/nesting_depth/react_hooks brace loops | 4 re-implemented brace-depth finders. Low-priority shared-util refactor; semantically equivalent, not byte-identical. |
| RQ-DUP-03 | server terminal_validation_test.rs vs integration/terminal_ws_test.rs | Same `validate_terminal_id` tested twice; keep richer terminal_ws_test.rs (asserts error message). Low priority. |
| — | rust/todo_comments.rs:53 | Regex recompile per file; minor perf (regex crate fast on short patterns). Low priority, part of regex-recompile cluster (RQ-06/RQ-10). |

## Cross-link clusters
- **regex-recompile cluster**: RQ-06 (frontend), RQ-10 (coverage_parser), rust/todo_comments.rs:53 — same anti-pattern (Regex::new in hot path vs crate's OnceLock convention). Fix together.
- **sonar cluster**: RQ-07 (dead param), RQ-13 (dead pub API), RQ-14 (whole provider dubious) — decide provider fate before micro-fixing.
- **gate-Warn cluster**: RQ-16 (warn ctor), RQ-18 (reserved MetricKeys), RQ-21 (MeasureValue::None) — all reserved-but-unwired Sonar-pedigree completeness; depends on whether shadow/warn folds into gate model.
- **policy-duplication cluster**: RQ-19 (build.rs FALLBACK_POLICY), RQ-20 (config.rs default_config) — three drifting copies of the gate policy vs quality-gate.yaml source of truth; risk: re-introducing eslint gating / silent lenient downgrade.
- **weak-default-security**: RQ-08 (file-filter bug) + RQ-09 (no dedicated metric) — same security feature half-wired.
- **gate-condition cleanup**: RQ-02, RQ-03, RQ-17 — same file (gate/condition.rs) safe deletes/investigates.

## Invisible features (preserve — load-bearing invariants)
- **ESLint advisory cap** (frontend.rs L200-214): EslintErrors pinned to 0; any gate YAML condition on EslintErrors>0 is a no-op by construction. Also enforced in SARIF path via `cap_for_advisory` (sarif.rs, tested). RQ-19 risks violating this.
- **UNAVAILABLE_RULE_SUFFIX `::unavailable`** (frontend.rs): consumed at agent.rs:9651 to distinguish env-blockers from code-blockers. Renaming breaks orchestrator bootstrap classifier.
- **Node subprocess port-stripping** (PORT/BACKEND_PORT/FRONTEND_PORT): R6 fix in mod.rs/sonar.rs/rust_analyzer.rs; tested. Removal reintroduces Windows port-orphan incident.
- **Anti-stub bypass gate** (RQ-12): security-critical, runs every discover(); refactor not delete.
- **Windows resolve_node_exe PATH shim** (R5 Fix 6 v3): prevents CreateProcessW silent-fail on bare tool names; used at agent.rs:9569.
- **Empty-scan fail-closed sentinel** (MetricKey::QualityGateEmptyScan): forces Blocker on vacuous green; 4 tests. KEEP.
- **Evaluator fail-closed sentinels** (evaluator.rs:39-70): missing metric + Int(-1) → ERROR; tested (G33-001). KEEP.
- **FrontendTestDepsMissing coherence check** (Fix #4): blocking Critical on missing curated test-infra deps. KEEP.
