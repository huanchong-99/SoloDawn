# rs-utils Census — crates/utils/src/

Branch: refactor/streamline-quality-gates  
Date: 2026-06-14

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|---|---|---|---|---|
| `lib.rs` | Crate root; WSL2 detection, cache-dir lookup, PowerShell script extractor | `is_wsl2()`, `cache_dir()`, `get_powershell_script()`, re-exports all sub-modules | `is_wsl2` → browser.rs, services/notification.rs; `cache_dir` → services/config/versions/v2.rs, services/image.rs, server/routes/planning_drafts.rs; `get_powershell_script` → services/notification.rs | `get_powershell_script` embeds and caches a toast-notification.ps1 asset at runtime |
| `api/mod.rs` | Re-exports oauth, organizations, projects sub-modules | — | — | Pure re-export |
| `api/oauth.rs` | Shared OAuth/auth wire types for TS binding generation | `HandoffInitRequest/Response`, `HandoffRedeemRequest/Response`, `TokenRefreshRequest/Response`, `ProviderProfile`, `ProfileResponse`, `LoginStatus`, `StatusResponse` | Used by server/routes/oauth.rs, generate_types.rs, services/auth.rs | `HandoffInit*` and `Handoff/TokenRefresh*` types referenced only in generate_types.rs (type export) and server/routes/oauth.rs (response body). `HandoffInitRequest` / `HandoffRedeemRequest` / `TokenRefreshRequest` are NOT imported outside generate_types — likely client→server request shapes. |
| `api/organizations.rs` | Shared org/member/invitation wire types | `MemberRole`, `InvitationStatus`, `Organization`, `OrganizationWithRole`, `ListOrganizationsResponse`, `GetOrganizationResponse`, `CreateOrganizationRequest/Response`, `UpdateOrganizationRequest`, `Invitation`, `CreateInvitationRequest/Response`, `ListInvitationsResponse`, `GetInvitationResponse`, `AcceptInvitationResponse`, `RevokeInvitationRequest`, `OrganizationMember`, `OrganizationMemberWithProfile`, `ListMembersResponse`, `UpdateMemberRoleRequest/Response` | generate_types.rs exports all; api/projects.rs imports `OrganizationMemberWithProfile` | Large shared-types file; all used by generate_types.rs |
| `api/projects.rs` | Remote project wire types | `RemoteProject`, `ListProjectsResponse`, `RemoteProjectMembersResponse` | generate_types.rs, services/project.rs imports `RemoteProject` | `ListProjectsResponse` only referenced from generate_types.rs — no production import found |
| `approvals.rs` | Tool-call approval protocol types shared across crates | `APPROVAL_TIMEOUT_SECONDS`, `ApprovalRequest`, `CreateApprovalRequest`, `ApprovalStatus`, `ApprovalResponse` | Heavily used: services/approvals.rs, executors (claude, codex, opencode, acp, etc.), server/routes/approvals.rs | Core feature; actively used across the entire approval subsystem |
| `assets.rs` | Embed + locate static assets (sounds, scripts, config/credentials paths) | `asset_dir()`, `config_path()`, `profiles_path()`, `credentials_path()`, `SoundAssets`, `ScriptAssets` | Uses `env_compat::var_opt_with_compat`; `ScriptAssets` used by lib.rs `get_powershell_script`; `SoundAssets` by services/config/versions/v2.rs | `GITCORTEX_ASSET_DIR` compat still wired through env_compat |
| `browser.rs` | Cross-platform URL opener (WSL2-aware) | `open_browser(url)` | Uses lib.rs `is_wsl2`; callers: server/main.rs, server/routes/task_attempts/pr.rs | Thin wrapper; tray/src/main.rs has its own `open_browser` method (not this fn) |
| `diff.rs` | Unified diff generation and parsing utilities | `FileDiffDetails`, `Diff`, `DiffChangeKind`, `create_unified_diff()`, `compute_line_change_counts()`, `extract_unified_diff_hunks()`, `concatenate_diff_hunks()`, `normalize_unified_diff()` | Used by services/git.rs, services/diff_stream.rs, executors/claude.rs, executors/codex/normalize_logs.rs; type-exported via generate_types.rs | `FileDiffDetails` only used in services/git.rs; active |
| `env_compat.rs` | GITCORTEX_ → SOLODAWN_ env var backward-compat shim; scheduled for removal at v0.2.0 | `var_with_compat()`, `var_is_set()`, `var_opt_with_compat()` | Callers: path.rs, assets.rs, deployment/lib.rs, server/main.rs, server/middleware/auth.rs, server/routes/* (>15 call sites), services/* (>10 call sites) | SCHEDULED REMOVAL — still has 30+ call sites; cannot delete until all callers migrated |
| `git.rs` | Git branch name validation | `is_valid_branch_prefix()` | Used by server/routes/config.rs | Thin wrapper over `git2::Branch::name_is_valid` |
| `jwt.rs` | JWT signature verification and claim extraction | `TokenClaimsError`, `verify_and_extract_expiration()`, `verify_and_extract_subject()` | Only used within own test module — no production caller found in the codebase | SUSPECT: no non-test callers found; may be dead or used by a crate not searched |
| `lib.rs` → `log_msg.rs` | SSE/WebSocket message type enum with serialization | `EV_*` constants, `LogMsg`, `to_sse_event()`, `to_ws_message()`, `to_ws_message_unchecked()`, `approx_bytes()` | Used by msg_store.rs; executors via workspace_utils | Core streaming infrastructure |
| `msg_store.rs` | Thread-safe broadcast ring-buffer for execution log messages | `MsgStore`, `push*`, `get_receiver()`, `get_history()`, `history_plus_stream()`, `sse_stream()`, `stdout/stderr_*_stream()`, `spawn_forwarder()` | Used by all executors, services; `LinesStreamExt` from stream_lines | Active core infrastructure |
| `path.rs` | Path utilities; worktree-relative path resolution, temp-dir, macOS alias normalization | `VIBE_IMAGES_DIR`, `make_path_relative()`, `normalize_macos_private_alias()`, `get_solodawn_temp_dir()`, `get_gitcortex_temp_dir()` (deprecated), `expand_tilde()` | `get_solodawn_temp_dir` used by port_file.rs, local-deployment/container.rs, executors/copilot.rs; `make_path_relative` by executors/claude.rs, executors/acp/normalize_logs.rs, executors/opencode/normalize_logs.rs; `expand_tilde` by services/repo.rs; `VIBE_IMAGES_DIR` by services/image.rs, server/routes/images.rs | `get_gitcortex_temp_dir` is deprecated alias — 0 external callers found (safe to delete) |
| `port_file.rs` | Async read/write of `<appname>.port` file in temp dir | `write_port_file()`, `read_port_file()` | `write_port_file` by server/main.rs; `read_port_file` by server/bin/mcp_task_server.rs | Used for IPC between server and MCP task server |
| `response.rs` | Generic `ApiResponse<T, E>` wrapper for HTTP responses | `ApiResponse<T,E>` with `success()`, `error()`, `error_with_data()`, `is_success()`, `into_data()`, `message()` | Used by server/error.rs, server/routes/concierge.rs (and others via use utils::response::ApiResponse); type-exported via generate_types.rs | Active |
| `sentry.rs` | Sentry error-tracking initialization and tracing layer | `SentrySource`, `init_once()`, `configure_user_scope()`, `sentry_layer()` | Used by server/main.rs, server/bin/mcp_task_server.rs, deployment/lib.rs | DSN hard-coded as fallback; overridable via `SENTRY_DSN` env var |
| `shell.rs` | Cross-platform shell detection, PATH refresh, executable resolution | `get_shell_command()`, `resolve_executable_path()`, `resolve_executable_path_blocking()`, `merge_paths()`, `UnixShell` enum | `resolve_executable_path_blocking` by all executors (claude, codex, gemini, amp, qwen, opencode, cursor, copilot), services/git/cli.rs; `get_shell_command` by executors/actions/script.rs, executors/qa_mock.rs; Windows registry PATH refresh path | Active; Windows registry code path is invisible feature |
| `stream_lines.rs` | Extension trait to convert chunked string streams to line streams | `LinesStreamExt` trait + blanket impl | Used by msg_store.rs, executors/acp/harness.rs | Simple adapter trait; active |
| `text.rs` | Text transformation utilities | `git_branch_id()`, `short_uuid()`, `truncate_to_char_boundary()` | `git_branch_id` + `short_uuid` by services/container.rs, local-deployment/container.rs; `truncate_to_char_boundary` by local-deployment/container.rs | Active |
| `tokio.rs` | Process-global Tokio runtime for sync→async bridging | `block_on()` | Used by shell.rs `resolve_executable_path_blocking` | Invisible feature: global multi-thread runtime never dropped; intentional leak per W2-36-07 comment |
| `url.rs` | LLM API endpoint resolution; deprecated URL normalizer | `ApiFormat`, `ResolvedEndpoint`, `resolve_endpoint()`, `normalize_base_url()` (deprecated) | `resolve_endpoint` used by services/orchestrator/llm.rs; `normalize_base_url` imported in services/orchestrator/llm.rs with `#[allow(deprecated)]` — test-only usage; `ApiFormat` used alongside `resolve_endpoint` | `normalize_base_url` deprecated since 0.0.154; still imported in llm.rs but only in test functions |
| `version.rs` | Exposes crate version as constant | `APP_VERSION` | Used by local-deployment/lib.rs | Single-line |

## Candidates

See structured output.
