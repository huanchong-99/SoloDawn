# Census: rs-db-models-B

Scope: `crates/db/src/models/` (alphabetical second half: project_repo through workspace_repo), plus `crates/db/src/lib.rs`, `crates/db/src/encryption.rs`, `crates/db/src/models/mod.rs`.

toolNotes: fast-context MCP returned resource_exhausted errors on all queries; all cross-file usage verified with Grep fallback.

---

## Module Map

| File | Purpose | Public Surface (types / fns) | Key Relations | Notes |
|------|---------|------------------------------|---------------|-------|
| `crates/db/src/lib.rs` | DB pool init, migration runner, schema self-healer | `DBService { pool }`, `DBService::new()`, `DBService::new_with_after_connect()` | Uses all models in integration tests; calls `run_migrations`, `verify_schema` | Windows CRLF checksum fix; `SCHEMA_EXPECTATIONS` auto-heals missing columns at startup |
| `crates/db/src/encryption.rs` | AES-256-GCM encrypt/decrypt for API keys | `encrypt(plaintext)`, `decrypt(encoded)`, `get_encryption_key()` | Called by `Workflow`, `Terminal` (and formerly concierge, feishu, planning_draft per header comment) | Legacy env var `GITCORTEX_ENCRYPTION_KEY` still supported with warn. Centralized refactor, was duplicated. |
| `crates/db/src/models/mod.rs` | Re-exports all model submodules | `pub use cli_type::*`, `concierge::*`, `git_event::*`, `orchestrator_message::*`, `quality_issue::*`, `quality_policy_snapshot::*`, `quality_run::*`, `system_settings::SystemSetting`, `terminal::*`, `workflow::*` | Barrel module for entire models layer | Does NOT glob-export workspace, task, session, repo, scratch, tag, project_repo, workspace_repo — callers must path-qualify those |
| `crates/db/src/models/project_repo.rs` | Junction table: project ↔ repo M:N | `ProjectRepo`, `CreateProjectRepo`, `ProjectRepo::find_by_project_id`, `find_by_repo_id`, `find_project_ids_by_repo_id`, `find_repos_for_project`, `find_by_project_and_repo`, `add_repo_to_project`, `remove_repo_from_project`, `create` | Depends on `Repo`; called by `server/routes/projects.rs` | `ProjectRepoError::AlreadyExists` guards duplicate inserts |
| `crates/db/src/models/quality_issue.rs` | Quality gate: individual linting/analysis issues per run | `QualityIssueRecord`, `SeverityCount`; `insert`, `insert_batch`, `find_by_run`, `find_blocking_by_run`, `count_by_severity`, `delete_by_run` | Referenced by `server/routes/quality.rs`, `services/orchestrator/agent.rs` | Part of Quality Gate System A; re-exported via `mod.rs` glob |
| `crates/db/src/models/quality_policy_snapshot.rs` | Quality gate: config snapshot at run time (audit trail) | `QualityPolicySnapshot`; `new`, `insert`, `find_by_run_id` | Referenced by `server/routes/quality.rs`, `services/orchestrator/agent.rs` | Enables reproducible gate decisions even if config changes later; re-exported via `mod.rs` glob |
| `crates/db/src/models/quality_run.rs` | Quality gate: run lifecycle (pending→ok/warn/error/skipped) | `QualityRun`, `AgeBucketCount`; `new_pending`, `insert`, `set_running`, `complete`, `set_failed`, `find_by_workflow`, `find_by_terminal`, `find_latest_by_terminal`, `find_by_task`, `find_by_terminal_and_commit`, `find_by_id`, `backfill_legacy_workflows`, `cleanup_old_runs`, `count_runs_by_age` | Referenced by `server/routes/quality.rs`, `services/orchestrator/agent.rs` | `backfill_legacy_workflows` and `cleanup_old_runs` have NO callers outside this file (only definition); re-exported via `mod.rs` glob |
| `crates/db/src/models/repo.rs` | Git repository metadata | `Repo`, `UpdateRepo`, `RepoError`; `list_needing_name_fix`, `update_name`, `find_by_id`, `find_by_path`, `find_by_ids`, `find_or_create`, `delete_orphaned`, `list_all`, `update` | Used by `project_repo.rs`, `workspace_repo.rs`; called by `services/project.rs`, `services/container.rs`, `local-deployment` | `list_needing_name_fix` / `update_name` used for migration backfill of repo names |
| `crates/db/src/models/scratch.rs` | Ephemeral scratch pad for UI draft state | `Scratch`, `ScratchPayload`, `ScratchType`, `DraftFollowUpData`, `PreviewSettingsData`, `DraftWorkspaceData`, `DraftWorkspaceRepo`, `CreateScratch`, `UpdateScratch`; `create`, `find_by_id`, `find_all`, `update`, `delete`, `find_by_rowid` | Called by `server/routes/scratch.rs`, `services/queued_message.rs`, `local-deployment/container.rs` | `find_by_rowid` supports SQLite change notification events in `services/events.rs` |
| `crates/db/src/models/session.rs` | Workspace execution session | `Session`, `CreateSession`, `SessionError`; `find_by_id`, `find_by_workspace_id`, `find_latest_by_workspace_id`, `create`, `create_for_terminal` | Used by workspace/task/execution pipelines; `create_for_terminal` called only from `services/terminal/launcher.rs` | `terminal_id` field on Session is a denormalized link; `create_for_terminal` is workflow-terminal path only |
| `crates/db/src/models/system_settings.rs` | Key-value config store, Feishu feature flag | `SystemSetting`; `find_all`, `get`, `get_bool`, `set`, `is_feishu_enabled_sync`, `is_feishu_enabled` | Used by `server/routes/system_settings.rs`, `server/routes/feishu.rs`, `server/routes/health.rs`, `server/main.rs` | `GITCORTEX_FEISHU_ENABLED` legacy env var still supported; exported via `mod.rs` as named re-export |
| `crates/db/src/models/tag.rs` | User-defined tags (name + content store) | `Tag`, `CreateTag`, `UpdateTag`; `find_all`, `find_by_id`, `create`, `update`, `delete` | Called by `server/routes/tags.rs`; also referenced in `mcp/task_server.rs` | Simple CRUD; referenced in MCP task server for tag listing |
| `crates/db/src/models/task.rs` | Core task model (project tasks + workspace hierarchy) | `Task`, `TaskStatus`, `TaskWithAttemptStatus`, `TaskRelationships`, `CreateTask`, `UpdateTask`; `deserialize_some`, `to_prompt`, `parent_project`, `find_by_project_id_with_attempt_status`, `find_by_id`, `find_by_rowid`, `find_by_shared_task_id`, `find_all_shared`, `create`, `update`, `update_status`, `update_status_cas`, `update_parent_workspace_id`, `nullify_children_by_workspace_id`, `clear_shared_task_ids_for_remote_project`, `delete`, `delete_scoped`, `set_shared_task_id`, `batch_unlink_shared_tasks`, `find_children_by_workspace_id`, `find_relationships_for_workspace` | Widely used across server routes and services | `deserialize_some` is public utility re-used only in this file (`UpdateTask.parent_workspace_id`); M38 tri-state patch pattern |
| `crates/db/src/models/terminal.rs` | Workflow terminal (CLI agent slot) with lifecycle CAS | `Terminal`, `TerminalStatus`, `TerminalLog`, `TerminalLogType`, `TerminalDetail`; full CRUD + `set_starting`, `set_waiting`, `set_started`, `count_active_launch_slots`, `try_set_starting_with_global_limit`, `set_completed`, `set_completed_cas`, `set_completed_if_unfinished`, `reset_for_restart`, `update_status_cas`, `update_process`, `update_session`, `update_last_commit`, `cleanup_old_logs`, `cleanup_finished_workflow_logs`, `checkpoint_after_cleanup` | Called by ~17 files in services and server; glob-exported via `mod.rs` | `set_started` is an alias for `set_waiting` (backward-compat); `QualityPending` status bridges terminal to quality gate flow |
| `crates/db/src/models/workflow.rs` | Workflow + WorkflowTask + SlashCommandPreset + WorkflowCommand | `Workflow`, `WorkflowStatus`, `WorkflowTask`, `WorkflowTaskStatus`, `WorkflowWithCounts`, `SlashCommandPreset`, `WorkflowCommand`; request DTOs `CreateWorkflowRequest`, `CreateWorkflowTaskRequest`, `CreateTerminalRequest`, `OrchestratorConfig`, `TerminalConfig`, `InlineModelConfig`; DB ops; `set_api_key`, `get_api_key`, `set_ready`, `set_started`, `set_merging`, `set_merge_completed`, `set_completed_from_running`, `update_status_with_reason`, `create_with_tasks`, etc. | Core orchestration model; glob-exported; used in ~18 files | `audit_plan: Option<String>` column is the AuditPlan System B bridge (JSON copied from planning_draft at materialize). `WorkflowCommand` FK to `slash_command_preset` missing `ON DELETE CASCADE` (G15-010 TODO). |
| `crates/db/src/models/workflow_event.rs` | Audit log events per workflow | `WorkflowEvent`; `insert`, `find_by_workflow` | Called by `services/orchestrator/agent.rs`, `server/routes/workflows.rs` | Simple append-only event log; not glob-exported (callers must path-qualify) |
| `crates/db/src/models/workspace.rs` | Workspace (execution environment) with status and naming | `Workspace`, `WorkspaceWithStatus`, `CreateWorkspace`, `WorkspaceContext`, `ContainerInfo`, `AttemptResumeContext`, `CreateFollowUpAttempt`; full CRUD + `load_context`, `fetch_all`, `find_expired_for_cleanup`, `resolve_container_ref`, `resolve_container_ref_by_prefix`, `find_all_with_status`, `find_by_id_with_status`, `touch`, `set_archived`, `update`, `get_first_user_message`, `truncate_to_name`, `count_all` | Very widely used; called by `server/routes/containers.rs` for VS Code bridge | `resolve_container_ref_by_prefix` is the VS Code webview bridge: resolves a filesystem path (including parent-dir fallback) to workspace context |
| `crates/db/src/models/workspace_repo.rs` | Junction table: workspace ↔ repo M:N with target_branch | `WorkspaceRepo`, `CreateWorkspaceRepo`, `RepoWithTargetBranch`, `RepoWithCopyFiles`; `create_many`, `find_by_workspace_id`, `find_repos_for_workspace`, `find_repos_with_target_branch_for_workspace`, `find_by_workspace_and_repo_id`, `update_target_branch`, `update_target_branch_for_children_of_workspace`, `find_unique_repos_for_task`, `find_repos_with_copy_files` | Used by `workspace.rs`, `local-deployment/container.rs` | `RepoWithCopyFiles` only used by `local-deployment` container setup |

---

## Candidates

See JSON output.
