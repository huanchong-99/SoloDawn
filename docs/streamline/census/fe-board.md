# Census: fe-board — frontend/src/components/board/

Generated by census agent on 2026-06-14. Branch: refactor/streamline-quality-gates.

## Module Map

| File | Purpose | Public Surface | Key Relations | Notes |
|------|---------|----------------|---------------|-------|
| StatusBar.tsx | Bottom status strip showing orchestrator on/off, running terminal count, WS connection state | `StatusBar({ workflowId })` | Reads `useWorkflow`, `useWsStore`; rendered by Board.tsx | "Tokens: N/A" is a hardcoded placeholder stub — no token-tracking hook exists yet |
| StatusBar.test.tsx | Unit tests for StatusBar | test suite | Mocks `useWorkflow`, `useWsStore` | 2 test cases covering active/inactive orchestrator |
| TaskCard.tsx | Draggable kanban task card with debug-navigate button and terminal status dots | `TaskCard({ task, workflowId })` | Uses `useDraggable` (@dnd-kit/core), renders `TerminalDots`; used by WorkflowKanbanBoard | Passes both `terminalCount` AND `terminals` to TerminalDots; only `terminals` branch is exercised in prod |
| TaskCard.test.tsx | Unit tests for TaskCard | test suite | Mocks nothing; uses MemoryRouter | 1 test case |
| TerminalActivityPanel.tsx | Collapsible panel listing active terminals with recent output lines; links to /debug route | `TerminalActivityPanel({ workflowId })` | Uses `useWorkflow`, `useRecentTerminalOutput`; rendered by Board.tsx | `ACTIVE_STATUSES` includes `'running'` which is NOT a valid `TerminalStatus` in shared/types.ts (backend maps it to `'working'`); dead member in the set |
| TerminalActivityPanel.test.tsx | Unit tests for TerminalActivityPanel | test suite | Mocks `useWorkflow`, `useRecentTerminalOutput`, i18n, react-router-dom | Minimal: only 1 test case covering null workflowId |
| TerminalDots.tsx | Visual row of colored dots representing terminal statuses; supports `quality_pending`, `review_passed`, `review_rejected` | `TerminalDots({ terminalCount?, terminals? })` | Used only by TaskCard.tsx (production) | `terminalCount` fallback branch dead in production (TaskCard always passes `terminals`); only exercised in TerminalDots.test.tsx |
| TerminalDots.test.tsx | Unit tests for TerminalDots | test suite | No mocks | Tests only the `terminalCount` fallback path — the status-colored `terminals` path has no dedicated test |
| WorkflowCard.tsx | Button card displaying workflow name+status in sidebar; selection state via `selected` prop | `WorkflowCard({ name, status, selected, onClick })` | Used only by WorkflowSidebar.tsx | Pure presentational; no hooks |
| WorkflowCard.test.tsx | Unit tests for WorkflowCard | test suite | No mocks | 2 test cases (render + selection styling) |
| WorkflowKanbanBoard.tsx | 6-column DnD kanban board for task status management; client-side transition whitelist | `WorkflowKanbanBoard({ workflowId })`, `KanbanColumn` (internal) | Uses `useWorkflow`, `useUpdateTaskStatus`, `@dnd-kit/core`; renders `TaskCard`; rendered by Board.tsx | G29-006 note: calls `useWorkflow` independently of Board.tsx causing redundant fetch; comment stubs for E09-03..07,08 (debounced mutations, unit tests) not yet implemented |
| WorkflowKanbanBoard.test.tsx | Unit tests for WorkflowKanbanBoard | test suite | Mocks `useWorkflow`, `useUpdateTaskStatus` | 7 test cases; well-covered |
| WorkflowSidebar.tsx | Left sidebar listing workflows for active project; project selector when >1 project | `WorkflowSidebar({ projects, activeProjectId, onProjectChange, selectedWorkflowId, onSelectWorkflow })` | Uses `useWorkflows`; renders `WorkflowCard`; uses `ui-new Button`, shadcn `Select`; navigates to /wizard | G29-010 note: could subscribe to WS `workflow.status_changed` directly for real-time badge updates |
| WorkflowSidebar.test.tsx | Unit tests for WorkflowSidebar | test suite | Mocks `useWorkflows` | 3 test cases |

## Candidates

| Path | Lines | Kind | Evidence | Disposition | Confidence |
|------|-------|------|----------|-------------|------------|
| TerminalDots.tsx | 60-72 | legacy | `terminalCount` fallback branch: only production caller (TaskCard) always passes `terminals`, so count-only path never runs. Only exercised by TerminalDots.test.tsx with `terminalCount={3}`. | refactor (remove fallback + update test) | medium |
| TerminalActivityPanel.tsx | 22 | bug | `'running'` in ACTIVE_STATUSES is not in backend TerminalStatus enum (`shared/types.ts` line 688). Backend maps old `running` -> `working`. Dead value that can never match. | refactor (remove 'running') | high |
| TerminalActivityPanel.tsx | 69 | bug | `StatusIndicator` renders `bg-green-500 animate-pulse` for `status === 'running'` — same dead check; backend never sends `running` for terminals. | refactor (remove `|| status === 'running'`) | high |
| StatusBar.tsx | 45 | stub | `t('statusBar.tokensNA')` always renders "Tokens: N/A" — no token-count hook or backend data source exists; comment in i18n key is a placeholder | investigate (wire up or intentionally keep as MVP stub) | medium |

## Invisible Features

| Feature | What It Does | Seems Used | Note |
|---------|-------------|------------|------|
| `quality_pending` status in TerminalDots | Yellow-pulsing dot for Quality Gate pending state | Yes — backend sends `quality_pending` per shared/types.ts | Relates to Quality Gate System A (G-series); surfaced as visual only, no click action |
| `review_passed`/`review_rejected` status in TerminalDots | Green/red static dot for quality review outcomes | Yes — in shared TerminalStatus enum | Part of QualityGate + AuditPlan System B signals |
| `agent_planned` executionMode in WorkflowKanbanBoard | Shows different empty-state copy when workflow uses agent-planned execution | Yes — WorkflowKanbanBoard.tsx:188 | Invisible divergence: users see different text but no other UI change |
| G29-010 stub (WorkflowSidebar) | NOTE comment: sidebar could subscribe to `workflow.status_changed` WS events for real-time badge updates | Not implemented | Improvement stub; sidebar currently waits for Board.tsx's 300ms debounced invalidation |
| E09-03..08 stub (WorkflowKanbanBoard) | Comment block for debounced drag mutations and drag-end unit tests | Not implemented | Tracked tasks not yet done; drag works but no debouncing on rapid redrags |
| Board.tsx quality gate handler | `handleQualityGateResult` invalidates `qualityKeys.*` on `quality.gate_result` WS events | Yes — Board.tsx:121-142 | Board is the WS hub for Quality Gate result propagation; board components themselves are passive |
