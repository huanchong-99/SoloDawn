# rs-executors-rest Module Census

Generated: 2026-06-14

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|------|---------|----------------|-----------|-------|
| `crates/executors/Cargo.toml` | Package manifest | — | Deps: tokio, serde, axum, agent-client-protocol, codex-protocol, reqwest, enum_dispatch, bon, fork_stream, os_pipe | `qa-mode` feature flag gates `QaMockExecutor` |
| `crates/executors/src/lib.rs` | Crate root | Re-exports all submodules | Exposes: actions, approvals, command, env, executors, logs, mcp_config, profile, stdout_dup | — |
| `crates/executors/src/executors/mod.rs` | Executor trait + enum dispatch | `StandardCodingAgentExecutor` trait, `CodingAgent` enum (9 variants), `BaseCodingAgent` discriminant, `SpawnedChild`, `AppendPrompt`, `ExecutorError`, `AvailabilityInfo`, `ExecutorExitResult`, `InterruptSender`, `build_review_prompt` | Called from: server routes (task_attempts, sessions), mcp/task_server; uses: approvals, command, env, profile, actions | CURSOR backward-compat alias kept via `#[serde(alias = "CURSOR")]` |
| `crates/executors/src/actions/mod.rs` | Action envelope + dispatch | `ExecutorAction`, `ExecutorActionType` (4 variants), `Executable` trait, `validate_working_dir`, `MAX_ACTION_DEPTH = 64` | Callers: server route layer; implemented by coding_agent_initial, coding_agent_follow_up, script, review | `append_action` guards against chains >64 |
| `crates/executors/src/actions/coding_agent_initial.rs` | Initial agent spawn action | `CodingAgentInitialRequest` (pub struct + `Executable` impl) | Loads profile from `ExecutorConfigs::get_cached()`, calls `agent.spawn()` | qa-mode branch uses `QaMockExecutor` |
| `crates/executors/src/actions/coding_agent_follow_up.rs` | Follow-up agent spawn action | `CodingAgentFollowUpRequest` (pub struct + `Executable` impl) | Same pattern as initial; calls `agent.spawn_follow_up()` | — |
| `crates/executors/src/actions/review.rs` | Code review spawn action | `ReviewRequest`, `RepoReviewContext` (pub structs + `Executable` impl) | Calls `agent.spawn_review()` | `#[cfg(not(feature = "qa-mode"))]` NOT present here — always live |
| `crates/executors/src/actions/script.rs` | Shell script action | `ScriptRequest`, `ScriptRequestLanguage`, `ScriptContext` (pub) | Spawns shell via `get_shell_command()`, no executor involved | Setup/cleanup/dev-server scripts |
| `crates/executors/src/approvals.rs` | Approval abstraction | `ExecutorApprovalService` trait, `NoopExecutorApprovalService`, `ExecutorApprovalError`, `ToolCallMetadata` | Used by: ClaudeCode, Gemini, QwenCode, Opencode, AcpAgentHarness | `NoopExecutorApprovalService` always approves |
| `crates/executors/src/command.rs` | Command-builder utilities | `CommandBuilder`, `CommandParts`, `CmdOverrides`, `CommandBuildError`, `apply_overrides` | Used by all executor `build_command_builder()` methods | Windows uses `winsplit`, Unix uses `shlex` |
| `crates/executors/src/env.rs` | Execution environment sanitization | `ExecutionEnv` (new/insert/merge/with_overrides/with_profile/apply_to_command) | Applied by all executors before spawning child processes | Blocks LD_PRELOAD/NODE_OPTIONS etc.; strips PORT/BACKEND_PORT/FRONTEND_PORT (R6 root-cause fix) |
| `crates/executors/src/profile.rs` | Executor profile registry | `ExecutorConfigs` (load/reload/get_cached/save_overrides/get_coding_agent/get_recommended), `ExecutorProfileId`, `ExecutorConfig`, `ProfileError`, `canonical_variant_key`, `to_default_variant` | Used by actions to look up agent config; server config routes for CRUD | Loads `default_profiles.json` + user overrides; `de_base_coding_agent_kebab` is a legacy compat shim |
| `crates/executors/src/mcp_config.rs` | MCP config read/write + adapter | `McpConfig` (pub struct), `read_agent_config`, `write_agent_config`, `PRECONFIGURED_MCP_SERVERS` (static), `CodingAgent::preconfigured_mcp` | Server config routes; each executor implements `default_mcp_config_path()` | Per-agent adapters: Passthrough, Gemini, Cursor, Codex, Opencode, Copilot |
| `crates/executors/src/stdout_dup.rs` | Stdout tee / pipe utilities | `duplicate_stdout`, `tee_stdout_with_appender`, `create_stdout_pipe_writer`, `StdoutAppender` | Used by: ClaudeCode (pipe writer), Copilot (tee+appender), Opencode (pipe writer) | Cross-platform FD manipulation (Unix OwnedFd / Windows OwnedHandle) |
| `crates/executors/src/executors/claude.rs` | Claude Code executor (main + log processor) | `ClaudeCode` (pub struct + `StandardCodingAgentExecutor`), `ClaudeLogProcessor::process_logs`, `HistoryStrategy`, `ClaudeJson` enum, `ClaudeToolData`, `ClaudeContentItem`, `ClaudeStreamEvent`, `ClaudeMessage`, `AppendPrompt` | Log processor reused by: Amp, QaMock; types reused by qa_mock | 2406 lines; streams JSON with `--output-format=stream-json`; control protocol via stdin/stdout |
| `crates/executors/src/executors/claude/client.rs` | Claude control protocol client | `ClaudeAgentClient::new`, `on_can_use_tool`, `on_hook_callback`, `on_non_control`, `AUTO_APPROVE_CALLBACK_ID` | Used by `ProtocolPeer::spawn` | Handles approval dispatch + hook callbacks |
| `crates/executors/src/executors/claude/protocol.rs` | Claude bidirectional protocol peer | `ProtocolPeer::spawn`, `send_user_message`, `initialize`, `set_permission_mode`, `interrupt` | Spawns read-loop task; calls `ClaudeAgentClient` methods | Handles interrupt signal via oneshot |
| `crates/executors/src/executors/claude/types.rs` | Claude control protocol types | `CLIMessage`, `SDKControlRequest`, `ControlRequestType`, `ControlResponseType`, `PermissionResult`, `PermissionMode`, `PermissionUpdate`, `Message` | Used by claude.rs, client.rs, protocol.rs | AcceptEdits variant defined but not used in spawn logic |
| `crates/executors/src/executors/amp.rs` | Sourcegraph Amp executor | `Amp` (pub struct + `StandardCodingAgentExecutor`) | Reuses `ClaudeLogProcessor` with `AmpResume` strategy; uses `npmx @sourcegraph/amp` | Fork/continue thread mechanism for session follow-up |
| `crates/executors/src/executors/gemini.rs` | Google Gemini CLI executor | `Gemini` (pub struct + `StandardCodingAgentExecutor`) | Delegates to `AcpAgentHarness`; re-exports `AcpAgentHarness` for use by qwen | ACP-based; `--experimental-acp` flag |
| `crates/executors/src/executors/qwen.rs` | Qwen Code executor | `QwenCode` (pub struct + `StandardCodingAgentExecutor`) | Uses `AcpAgentHarness::with_session_namespace("qwen_sessions")` via `gemini::AcpAgentHarness` | ACP-based; similar to Gemini |
| `crates/executors/src/executors/codex.rs` | OpenAI Codex executor | `Codex` + setup helper action | Uses codex-protocol + codex-app-server-protocol crates; has `get_setup_helper_action` | External git repo dependency (pinned rev) |
| `crates/executors/src/executors/cursor.rs` | Cursor IDE agent executor | `CursorAgent`, `CursorJson`, `CursorToolCall`, all typed result structs | Calls `mcp::ensure_mcp_server_trust` before spawn; custom log parser | 1358 lines; `cursor-agent` binary; `SetupHelper` capability |
| `crates/executors/src/executors/cursor/mcp.rs` | Cursor MCP auto-trust bootstrapper | `ensure_mcp_server_trust` (pub), `compute_cursor_approval_id`, `cursor_project_slug` | Called by `CursorAgent::spawn` and `spawn_follow_up` | Writes `~/.cursor/projects/<slug>/mcp-approvals.json` |
| `crates/executors/src/executors/copilot.rs` | GitHub Copilot executor | `Copilot` (pub struct + `StandardCodingAgentExecutor`) | Uses `tee_stdout_with_appender`; scans log dir for session UUID | Session discovery via filesystem polling of `--log-dir` |
| `crates/executors/src/executors/droid.rs` | Factory.ai Droid executor | `Droid`, `Autonomy`, `ReasoningEffortLevel` | Delegates log to `normalize_logs::normalize_logs`; session fork via `session::fork_session` | `droid exec --output-format stream-json` |
| `crates/executors/src/executors/droid/normalize_logs.rs` | Droid log normalization | `normalize_logs` (pub fn), `DroidJson`, `DroidToolData`, all state types | Used by `Droid::normalize_logs` | Contains `pub struct EditToolResult {}` — empty struct, unused |
| `crates/executors/src/executors/droid/session.rs` | Droid session fork | `fork_session` (pub fn) | Called by `Droid::spawn_follow_up` | Copies JSONL session file with new UUID |
| `crates/executors/src/executors/opencode.rs` | OpenCode.ai executor | `Opencode` (pub struct + `StandardCodingAgentExecutor`) | Uses HTTP SDK (`sdk::run_session`); waits for server URL on stdout; uses `exit_signal` + `interrupt_sender` | Starts a local server then drives it via REST API |
| `crates/executors/src/executors/opencode/normalize_logs.rs` | OpenCode log normalizer | `normalize_logs` (pub fn) | Called by `Opencode::normalize_logs` | — |
| `crates/executors/src/executors/opencode/sdk.rs` | OpenCode REST SDK client | `run_session`, `LogWriter`, `RunConfig` | Used by `opencode.rs` | HTTP polling of local opencode server |
| `crates/executors/src/executors/opencode/types.rs` | OpenCode API types | Various serde types for opencode REST API | Used by opencode/sdk.rs | — |
| `crates/executors/src/executors/acp/mod.rs` | ACP event types + re-exports | `AcpEvent`, `ApprovalResponse`, re-exports `AcpClient`, `AcpAgentHarness`, `SessionManager`, `normalize_logs::*` | Foundation for Gemini/Qwen ACP harness | `agent_client_protocol` crate (v0.8 unstable) |
| `crates/executors/src/executors/acp/harness.rs` | ACP harness (spawn/follow-up) | `AcpAgentHarness::new`, `with_session_namespace`, `spawn_with_command`, `spawn_follow_up_with_command` | Used by Gemini and Qwen | Handles ACP approval loop + exit signal |
| `crates/executors/src/executors/acp/client.rs` | ACP approval client | `AcpClient` | Used by `AcpAgentHarness` | Bridges ACP approval requests to `ExecutorApprovalService` |
| `crates/executors/src/executors/acp/session.rs` | ACP session manager | `SessionManager` | Used by `AcpAgentHarness` for session storage | — |
| `crates/executors/src/executors/acp/normalize_logs.rs` | ACP log normalizer | `normalize_logs` (pub fn) | Called by Gemini and Qwen via `super::acp::normalize_logs` | — |
| `crates/executors/src/executors/qa_mock.rs` | QA-mode mock executor | `QaMockExecutor` (cfg `qa-mode` feature) | Reuses `ClaudeLogProcessor::process_logs` | Performs actual random file ops + replays 10 fake log entries |
| `crates/executors/src/logs/mod.rs` | Log types | `NormalizedEntry`, `NormalizedEntryType`, `ToolStatus`, `ActionType`, `FileChange`, `ToolResult`, `CommandRunResult`, `NormalizedConversation` | Used by all log processors and server consumers | `#[ts(export)]` types drive TypeScript type gen |
| `crates/executors/src/logs/plain_text_processor.rs` | Generic streaming text log processor | `PlainTextLogProcessor` (builder), `MessageBoundary`, `FormatChunkFn`, `MessageBoundaryPredicateFn` | Used by: Copilot, Cursor (stderr), Droid (stderr), stderr_processor | Configurable size/time-gap/boundary splitting |
| `crates/executors/src/logs/stderr_processor.rs` | Standard stderr normalizer | `normalize_stderr_logs` (pub fn) | Used by Claude, Amp, Gemini, Qwen, Copilot, Codex | 2-second gap splits error messages |
| `crates/executors/src/logs/utils/mod.rs` | Log utils re-exports | Re-exports `EntryIndexProvider`, `ConversationPatch` | — | — |
| `crates/executors/src/logs/utils/entry_index.rs` | Atomic entry index counter | `EntryIndexProvider` (new/next/current/reset/start_from) | Used by all log processors | Shared via `Arc<AtomicUsize>`; `start_from` seeds from existing MsgStore patches |
| `crates/executors/src/logs/utils/patch.rs` | JSON patch helpers | `ConversationPatch` (add_normalized_entry/add_stdout/add_stderr/add_diff/replace/remove/etc.), `PatchType`, `extract_normalized_entry_from_patch`, `upsert_normalized_entry`, `add_normalized_entry`, `replace_normalized_entry` | Used by all log processors | `PatchType` includes Stdout/Stderr/Diff variants beyond NormalizedEntry |
| `crates/executors/default_mcp.json` | Bundled MCP server defaults | Static preconfigured MCP server definitions (embedded via `include_str!`) | Loaded by `PRECONFIGURED_MCP_SERVERS` static | Applies to all agents |
| `crates/executors/default_profiles.json` | Bundled default executor profiles (v3 flat) | JSON defaults for all 8+ agents | Loaded by `ExecutorConfigs::from_defaults()` | — |
