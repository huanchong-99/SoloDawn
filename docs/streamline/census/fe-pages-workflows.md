# Census: fe-pages-workflows

Unit covering `frontend/src/pages/` — all top-level workflow/board pages and settings.

## Module Map

| File | Purpose | Public Surface (exports) | Relations | Notes |
|------|---------|--------------------------|-----------|-------|
| `pages/Workflows.tsx` | Full-page workflow management: list + wizard + detail + orchestrator chat + prompt queue | `Workflows` (React component) | Routed at `/workflows` and `/wizard` from App.tsx. Imports: useWorkflows, useProjects, PipelineView, WorkflowWizard, WorkflowPromptDialog, qualityKeys. WS: useWorkflowEvents, useWsStore | 2012 lines; contains numerous module-private helpers: `getPromptContextKey`, `getPromptQueueItemId`, `cleanupPromptHistory`, `isSamePromptContext`, `getExecutionModeLabel`, `resolveProjectIdFromPath`, `WorkflowDetailActions`, `renderBlockingView`, `WORKFLOW_STATUS_MAP`, `WORKFLOW_STATUS_BADGE_CLASSES`, `mapWorkflowStatus`, `getWorkflowStatusBadgeClass`, `mapMergeTerminalStatus`, `mapWorkflowTasks`, `mapOrchestratorMessageRole`, `getOrchestratorChatHint`, `getOrchestratorMessageBubbleClass`, `renderOrchestratorConversationContent`, `hasConfiguredWorkflowModelLibrary`, `OrchestratorChatPanel`, `SelectedWorkflowView`, `WorkflowListContent` |
| `pages/Board.tsx` | Kanban board view: project/workflow selector, real-time WS invalidation, quality gate result handling | `Board` (React component) | Routed at `/board` (default index). Imports: WorkflowSidebar, WorkflowKanbanBoard, TerminalActivityPanel, StatusBar, qualityKeys, workflowKeys | Primary landing page. G29-008 debounced WS cache invalidation. G08-006 system.lagged handler. G31-007 quality gate run/issue cache invalidation |
| `pages/Pipeline.tsx` | Orchestrator pipeline view: thin wrapper loading workflow + displaying OrchestratorHeader + TaskPipeline | `Pipeline` (React component) | Routed at `/pipeline/:workflowId`. Imports: useWorkflow, useWorkflowInvalidation, OrchestratorHeader, TaskPipeline | 27 lines; minimal logic; delegates to pipeline components |
| `pages/WorkflowDebugPage.tsx` | PTY/terminal debug view: thin wrapper loading workflow tasks + dispatching to TerminalDebugView | `WorkflowDebugPage` (React component) | Routed at `/debug/:workflowId`. Imports: useWorkflow, useWorkflowInvalidation, TerminalDebugView. Navigated to from TerminalActivityPanel, TaskCard, NewDesignLayout | Constructs WebSocket URL for PTY connection; maps DTO tasks → Terminal types |
| `pages/SlashCommands.tsx` | CRUD management for user-defined slash command presets; renders card grid + inline form dialog | `SlashCommands` (React component) | Routed at `/commands` inside LegacyDesignScope/NormalLayout. Imports: useSlashCommands, useCreateSlashCommand, useUpdateSlashCommand, useDeleteSlashCommand | Uses old design system (legacy route). Module-private: `SlashCommandFormDialog` |
| `pages/ui-new/WorkspacesLanding.tsx` | Redirect shim: immediately redirects `/workspaces` → `/workspaces/create` | `WorkspacesLanding` (React component) | Routed at `/workspaces`. Only logic: `<Navigate to="/workspaces/create" replace />` | 5-line shim. Could be replaced by a router redirect |
| `pages/ui-new/Workspaces.tsx` | Workspace view: thin shell wrapping `WorkspacesLayout` container | `Workspaces` (React component) | Routed at `/workspaces/create` and `/workspaces/:workspaceId`. Imports: WorkspacesLayout | 5-line thin wrapper |
| `pages/ui-new/Assistant.tsx` | AI concierge chat page: thin shell wrapping `ConciergeChatContainer` | `Assistant` (React component) | Routed at `/assistant`. Imports: ConciergeChatContainer | 5-line thin wrapper |
| `pages/ui-new/FirstRunWizard.tsx` | Post-install first-run setup wizard (4 steps: Welcome, Environment, API Keys, Done) | `FirstRunWizard` (React component) | Routed at `/first-run`. Imports: useFirstRun, useRuntimeStatus (internal), SetupWizardStepIndicator. On finish: marks first run complete, navigates to /board | Step components are module-private: `WelcomeStep`, `EnvironmentStep`, `ApiKeyStep`, `DoneStep`. Fetches `/api/cli-types/detect` for environment status |
| `pages/settings/index.ts` | Re-export barrel: aliases `*New` components as plain names | Re-exports: `GeneralSettings`, `ModelsSettings`, `ProjectSettings`, `ReposSettings`, `AgentSettings`, `McpSettings`, `FeishuSettings`, `OrganizationSettings`, `SettingsLayout` | Consumed by: nothing in production code — App.tsx imports directly from `@/pages/ui-new/settings` | Barrel that no production caller uses; only `organizationRemoteCapability` is imported by a real consumer |
| `pages/settings/organizationRemoteCapability.ts` | Utility: detects "remote project/organization not supported" API errors by regex pattern match | `isRemoteProjectCapabilityUnsupported(error): boolean` | Imported by `OrganizationSettingsNew.tsx` (3 call sites) and its own test | Regex-based error classification — brittle if error message format changes |
| `pages/ui-new/settings/GeneralSettingsNew.tsx` | General settings page: theme, language, sound, branch prefix, PR description prompt, editor type | `GeneralSettingsNew` (React component) | Routed at `/settings/general`. Imports: useUserSystem, useEditorAvailability, EditorAvailabilityIndicator, TagManager | Contains editor type picker — relevant to G1 "open in external IDE" feature |
| `pages/ui-new/settings/AgentSettingsNew.tsx` | Agent executor configuration: profiles, executors, JSON editor | `AgentSettingsNew` (React component) | Routed at `/settings/agents`. Imports: useProfiles, configApi, ExecutorConfigForm, CreateConfigurationDialog, DeleteConfigurationDialog, AgentAvailabilityIndicator | NativeSelect inline primitive declared locally |
| `pages/ui-new/settings/McpSettingsNew.tsx` | MCP server configuration page; also exports `buildMcpServersPayload` utility | `McpSettingsNew` (React component), `buildMcpServersPayload` (exported utility fn) | Routed at `/settings/mcp`. `buildMcpServersPayload` imported by `pages/settings/McpSettings.test.ts` | `MCP_NOT_SUPPORTED_ERROR_CODE` const guards graceful error display |
| `pages/ui-new/settings/ModelsSettingsNew.tsx` | Workflow model library configuration (AI model API keys, providers) | `ModelsSettingsNew` (React component) | Routed at `/settings/models`. Imports: Step3Models wizard step, useUserSystem | Parses `workflow_model_library` from user config |
| `pages/ui-new/settings/ProjectSettingsNew.tsx` | Project management settings: create/rename/delete projects, set repos | `ProjectSettingsNew` (React component) | Routed at `/settings/projects`. Imports: useProjects, useProjectMutations, RepoPickerDialog, CreateProjectDialog | |
| `pages/ui-new/settings/ReposSettingsNew.tsx` | Repository settings: display name, setup/cleanup/dev server scripts, copy files | `ReposSettingsNew` (React component) | Routed at `/settings/repos`. Imports: repoApi, useRepoBranches, MultiFileSearchTextarea, useScriptPlaceholders | |
| `pages/ui-new/settings/FeishuSettingsNew.tsx` | Feishu (Lark) integration settings: app ID/secret, enable/disable, test send/receive | `FeishuSettingsNew` (React component) | Routed at `/settings/feishu`. Imports: feishuApi, makeRequest | Invisible feature: Feishu bot integration for notifications |
| `pages/ui-new/settings/OrganizationSettingsNew.tsx` | Organization management: members, invitations, remote projects, org creation | `OrganizationSettingsNew` (React component) | Routed at `/settings/organizations`. Imports: useUserOrganizations, useOrganizationSelection, useOrganizationMembers, useOrganizationInvitations, useOrganizationMutations, isRemoteProjectCapabilityUnsupported | Handles 501 "remote project unsupported" gracefully with InfoAlert |
| `pages/ui-new/settings/RuntimeSettingsNew.tsx` | CLI runtime status and installation detection | `RuntimeSettingsNew` (React component) | Routed at `/settings/runtime`. Imports: cliTypesKeys, handleApiResponse | Fetches `/api/cli_types/detect` (note: underscore URL vs FirstRunWizard's `/api/cli-types/detect` — inconsistency) |
| `pages/ui-new/settings/index.ts` | Export barrel for all `*New` settings components | Re-exports all `*New` settings components + `RuntimeSettingsNew` | Imported by App.tsx directly | |

## Test / non-production files

| File | Type | Notes |
|------|------|-------|
| `pages/Board.test.tsx` | Unit test | |
| `pages/Pipeline.test.tsx` | Unit test | |
| `pages/SlashCommands.test.tsx` | Unit test | |
| `pages/SlashCommands.e2e.test.tsx` | E2E-style test | |
| `pages/WorkflowDebugPage.test.tsx` | Unit test | Correct route `/debug/:workflowId` |
| `pages/__tests__/WorkflowDebugPage.test.tsx` | Unit test (duplicate) | Uses wrong route `/workflow/:workflowId/debug` — stale/incorrect path assumption |
| `pages/Workflows.test.tsx` | Unit test | |
| `pages/settings/McpSettings.test.ts` | Unit test for `buildMcpServersPayload` utility | |
| `pages/settings/organizationRemoteCapability.test.ts` | Unit test | |
