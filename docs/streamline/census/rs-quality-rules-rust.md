# Census: `crates/quality/src/rules/rust/` (rs-quality-rules-rust)

Generated by census agent. Unit: rs-quality-rules-rust. 14 files inspected.

## Module Map

| File | Purpose | Public Surface | Relations / Callers | Notes |
|------|---------|----------------|---------------------|-------|
| `mod.rs` | Re-exports all 13 rule sub-modules; exposes `all_rust_rules()` factory | `pub fn all_rust_rules() -> Vec<Box<dyn RustRule>>` | Called by `crates/quality/src/provider/builtin_rust.rs:57` (`BuiltinRustProvider::analyze`) | Entry point for the entire built-in Rust rule set |
| `clone_usage.rs` | Detects `.clone()` method calls via `syn::Visit` | `CloneUsageRule` (impl `Rule + RustRule`) | Registered in `all_rust_rules()`, analyzed by `BuiltinRustProvider` | Flags every `.clone()` with empty args; no per-file severity config override path |
| `cognitive_complexity.rs` | Calculates cognitive complexity for functions/methods; penalises nesting | `CognitiveComplexityRule` (default threshold=20) | Registered in `all_rust_rules()`; `rule_id.contains("cognitive")` picked up by `builtin_rust.rs:106` for `RustCognitiveComplexity` metric | Most complex analysis file (~525 lines). `_private: ()` field makes `#[derive(Default)]` safe while preventing direct struct literal construction |
| `cyclomatic_complexity.rs` | Cyclomatic complexity (CC = 1 + decision points) via `syn::Visit` | `CyclomaticComplexityRule` (default threshold=15) | Registered in `all_rust_rules()`; `rule_id.contains("cyclomatic")` picked up by `builtin_rust.rs:98` for `RustCyclomaticComplexity` metric; `builtin_rust_critical` referenced in `quality-gate.yaml` | Also feeds `quality-gate.yaml` metric `rust_cyclomatic_complexity` |
| `documentation.rs` | Flags public items (`fn`, `struct`, `enum`, `trait`, `type`) missing `///` doc comments | `DocumentationRule` (impl `Rule + RustRule`) | Registered in `all_rust_rules()` | Respects `#[allow(missing_docs)]` and `_`-prefixed names; does NOT check `pub(crate)` items differently from `pub` |
| `error_handling.rs` | Detects `.unwrap()`, `.expect()`, `panic!`, `unreachable!`, `unimplemented!`, `todo!` in production code | `ErrorHandlingRule` (impl `Rule + RustRule`); `rule_type=Bug`, `severity=Critical` | Registered in `all_rust_rules()`; contributes to `builtin_rust_critical` in `quality-gate.yaml` | **Bug**: `in_test: bool` field is initialised to `false` and never set to `true`. `self.in_test` guards at lines 112, 120, 135, 148 are permanently dead. Actual test-skipping works correctly via `has_test_attr` / `has_cfg_test_attr`. `content` field has `#[allow(dead_code)]` — it is stored but never read. |
| `file_length.rs` | Flags files exceeding `max_lines` (default 500) | `FileLengthRule` (impl `Rule + RustRule`) | Registered in `all_rust_rules()` | Text-based (uses `ctx.content.lines().count()`), not AST-based |
| `function_length.rs` | Flags functions/methods exceeding `max_lines` (default 60) via brace span arithmetic | `FunctionLengthRule` (impl `Rule + RustRule`) | Registered in `all_rust_rules()` | Uses `block.brace_token.span` open/close line numbers; covers both `ItemFn` and `ImplItemFn` |
| `magic_numbers.rs` | Detects integer/float literals that are not common exempt values (0, 1, 2, -1, 0.0, 1.0) outside `const`/`static`/array-index | `MagicNumbersRule` (impl `Rule + RustRule`) | Registered in `all_rust_rules()` | Exempt list is hardcoded; no config param to extend it |
| `naming.rs` | Checks `snake_case` (fns), `CamelCase` (types/traits), `UPPER_SNAKE_CASE` (const/static) | `NamingConventionRule` (impl `Rule + RustRule`); private helpers `is_snake_case`, `is_upper_camel_case`, `is_upper_snake_case` | Registered in `all_rust_rules()` | No enum variant naming check; does not visit `ImplItemFn` names or local variables |
| `nesting_depth.rs` | Measures per-function max control-flow nesting depth via two-layer visitor | `NestingDepthRule` (impl `Rule + RustRule`) (default `max_depth=5`) | Registered in `all_rust_rules()` | `FunctionNestingVisitor` stops at nested `ItemFn` to avoid double-counting |
| `todo_comments.rs` | Regex-based text scan for `TODO`, `FIXME`, `HACK`, `XXX` in `//` and `/* */` comments | `TodoCommentsRule` (impl `Rule + RustRule`); severity=`Info` | Registered in `all_rust_rules()` | Only rule that uses `regex` crate instead of AST visitor; `Regex::new(...).unwrap()` called on every `analyze()` invocation (minor perf waste; should be `OnceLock`) |
| `type_complexity.rs` | Measures generic nesting depth in type annotations (`fn` params/return, struct fields, `let` bindings) | `TypeComplexityRule` (impl `Rule + RustRule`) (default threshold=5) | Registered in `all_rust_rules()` | `visit_local` has a dead `let _ = init;` statement (comment says "used for visiting below" but `syn::visit::visit_local` handles that) |
| `unsafe_usage.rs` | Flags `unsafe` blocks, `unsafe fn`, `unsafe impl` as security hotspots | `UnsafeUsageRule` (impl `Rule + RustRule`); `rule_type=SecurityHotspot`, `severity=Major` | Registered in `all_rust_rules()`; contributes to `builtin_rust_critical` via security severity | `.with_context()` builder called here but not in other rules — suggests `QualityIssue::context` field mostly unused elsewhere |

## Cross-Cutting Notes

- **All 13 rule structs** are registered in `mod.rs::all_rust_rules()` and consumed by `BuiltinRustProvider` (enabled when `config.providers.builtin_rust=true`).
- Metrics `builtin_rust_issues`, `builtin_rust_critical`, `rust_cyclomatic_complexity` are referenced in `quality/quality-gate.yaml` (Quality Gate System A).
- `RustCognitiveComplexity` metric is produced but NOT referenced in `quality-gate.yaml` — invisible feature.
- No rule produces `AnalyzerSource::Clippy` or `AnalyzerSource::ExternalTool`; all use `AnalyzerSource::Other("built-in")`.

## Candidates Summary

| # | File | Lines | Kind | Confidence | Hint |
|---|------|-------|------|------------|------|
| 1 | `error_handling.rs` | 64-66 | bug | high | `in_test` field always `false`; `content` field dead (`#[allow(dead_code)]`) |
| 2 | `type_complexity.rs` | 202-205 | bug | medium | `let _ = init;` inside `visit_local` is a no-op dead statement |
| 3 | `todo_comments.rs` | 53 | dubious-feature | low | `Regex::new(...).unwrap()` per call — should be compiled once via `OnceLock` |
