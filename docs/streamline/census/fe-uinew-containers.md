# Census: fe-uinew-containers

Unit scope: `frontend/src/components/ui-new/containers/` (39 files). Architecture rule: containers manage state, pass to `views/`; `primitives/` are reusable presentational. Several "containers" here are actually small presentational primitives (file names PascalCase, scoped to `.new-design`).

Tooling note: `mcp__fast-context__fast_context_search` returned `resource_exhausted` on every attempt (5 calls, distinct trace IDs). All cross-file usage below was resolved with Grep over `frontend/src/**/*.{ts,tsx}` as the documented fallback.

## Module map

| File | Purpose | Public surface | Relations | Notes |
|---|---|---|---|---|
| AuditDocPanel.tsx | Right-side panel in planning mode to upload/remove an audit doc + toggle "retain built-in" audit. System B (planning-draft). | `AuditDocPanel(props)` | Rendered ONLY by CreateChatBoxContainer; uses `useUploadAuditDoc`/`useDeleteAuditDoc` from usePlanningDraft. auditMode `builtin`/`merged`/`custom`. | G2-CRITICAL. Live. Part of audit System B. |
| BrowseRepoButtonContainer.tsx | "Browse for repo" list item; opens FolderPickerDialog, registers repo. | `BrowseRepoButtonContainer({disabled,onRepoRegistered})` | Used by `views/GitPanelCreate.tsx`. `repoApi.register`. | Live. |
| ChangesPanelContainer.tsx | Container for the Changes (diff list) panel; intersection-observer file-in-view tracking, auto-collapse. | `ChangesPanelContainer({className,attemptId})` | Used by WorkspacesLayout; renders `views/ChangesPanel`; passes `attemptId` down to diff cards (IDE button surface). | Live. `attemptId` prop only feeds the open-in-IDE button (G1). |
| CollapsibleSection.tsx | Presentational collapsible section (persisted expand state). | `CollapsibleSection(props)` | Used by RepoCard. | Live primitive (misfiled as container). |
| CollapsibleSectionHeader.tsx | Header variant of collapsible with optional icon button. | `CollapsibleSectionHeader(props)` | Used by ProcessListContainer. | Live primitive. |
| CommentWidgetLine.tsx | Inline new-comment editor widget rendered in diff gutter. | `CommentWidgetLine(props)` | Used by DiffViewCardWithComments; uses ReviewProvider + WYSIWYGEditor. | Live (review System). |
| ConciergeChatContainer.tsx | AI concierge chat (WS-driven), sessions, feishu sync, sync toggles. | `ConciergeChatContainer({initialSessionId?})` | Used by WorkspacesLayout + `pages/ui-new/Assistant.tsx`; conciergeWsStore, useConcierge*. | Live. Invisible-ish feature (WS bridge + feishu). |
| ContextBarContainer.tsx | Wires action groups + editorType into the floating ContextBar. | `ContextBarContainer({containerRef})` | Used by `views/WorkspacesMain.tsx`; reads `config.editor.editorType`; reuses `filterVisibleItemPair` from NavbarContainer. | Live. `editorType` prop is part of G1 IDE-deletion surface (ContextBar `ide-icon`). |
| ConversationListContainer.tsx | Virtuoso message list rendering normalized conversation entries. | `ConversationList` (named + default export) | Used by `views/WorkspacesMain.tsx`; renders NewDisplayConversationEntry. | Live. File name (ConversationListContainer) != export name (ConversationList) — minor naming smell. |
| CopyButton.tsx | Copy-to-clipboard button with checkmark feedback. | `CopyButton({onCopy,disabled})` | Used by `primitives/ContextBar.tsx`. | Live primitive. |
| CreateChatBoxContainer.tsx | Main "Create workspace" chat: planning-draft create → send → confirm → materialize; planner model select; feishu sync; audit panel. | `CreateChatBoxContainer()` | Used by WorkspacesLayout (isCreateMode). usePlanningDraft hooks, planningDraftsApi, AuditDocPanel, WorkflowProgressContainer, CreateChatBox primitive. | G2-CRITICAL. Live. The canonical planning flow (System B). |
| CreateRepoButtonContainer.tsx | "Create new repo" list item; opens CreateRepoDialog. | `CreateRepoButtonContainer({onRepoCreated})` | Used by `views/GitPanelCreate.tsx`. | Live. |
| DiffViewCardWithComments.tsx | Diff card with inline review + GitHub comments, add-widget, Open-in-IDE button. | `DiffViewCardWithComments(props)`, `DiffInput` type, `useDiffData` (re-exported) | Used widely via ChangesPanel→ChangesPanelContainer; uses ReviewProvider, WorkspaceContext, CommentWidgetLine/ReviewCommentRenderer/GitHubCommentRenderer, OpenInIdeButton+useOpenInEditor. | G2-CRITICAL. Live. IDE button (L31-32,193-197,431-438) is documented G1 deletion call-site. |
| FeishuChannelContainer.tsx | Feishu channel binding panel: bind concierge session / planning draft to feishu chat. | `FeishuChannelContainer()` | Used by RightSidebar (3 branches). feishuApi, planningDraftsApi.toggleFeishuSync, useFeishuChannel. | Live. Invisible feature (Feishu sync). |
| FileTreeContainer.tsx | File tree of changed files with search, collapse, GitHub-comment toggle. | `FileTreeContainer(props)` | Used by RightSidebar (CHANGES mode); fileTreeUtils, WorkspaceContext, ChangesViewContext. | Live. |
| GitHubCommentRenderer.tsx | Read-only renderer for a GitHub PR comment in the diff gutter. | `GitHubCommentRenderer({comment})` | Used by DiffViewCardWithComments. | Live. |
| GitPanelContainer.tsx | Git status panel: per-repo push state, PR info, actions, open-in-editor, copy path, settings. | `GitPanelContainer(props)`, `GitPanelContainerProps` | Used by RightSidebar (all main modes). usePush/useRenameBranch/useBranchStatus, repoApi.openEditor, GitPanel view, RepoCard action type, Actions. | G2-CRITICAL. Live. `handleOpenInEditor`→`repoApi.openEditor` (L212-229) is G1 deletion call-site. |
| GitPanelCreateContainer.tsx | Create-mode repo binding: project select, auto-load bound repo, branch select. | `GitPanelCreateContainer({className})` | Used by RightSidebar (create mode); GitPanelCreate view, useCreateMode, repoApi.register. | Live. |
| InputField.tsx | Editable/search input primitive with save/cancel/feedback. | `InputField(props)` | Used by ProcessListContainer (+ views GitPanel, WorkspacesSidebar, FileTreeSearchBar, workflow Step0Project). | Live primitive. |
| LogsContentContainer.tsx | Renders process/tool logs via VirtualizedProcessLogs; search match indices. | `LogsContentContainer({className})`, `LogsPanelContent` type | Used by WorkspacesLayout (LOGS mode); useLogStream, LogsPanelContext. | Live. |
| NavbarContainer.tsx | Top navbar: filter visible actions, title. Exports shared divider-filter helpers. | `NavbarContainer()`, `filterVisibleItemsWithDividers`, `filterVisibleItemPair` | Used by WorkspacesLayout; helpers reused by ContextBarContainer. Navbar view, Actions. | Live. |
| NewDisplayConversationEntry.tsx | Maps a normalized conversation entry to the right chat primitive (tool/todo/file/approval/user/assistant/etc). | `NewDisplayConversationEntry` (default) | Used by ConversationListContainer; conversation primitives, ScriptFixerDialog, DiffViewCard (primitives DiffInput). | Live. Large (760L) — only first 80 lines inspected (mapping + diff-stat helpers); remainder is branch rendering. |
| PlanningChatContainer.tsx | Earlier/alt planning-chat container: create draft → confirm → materialize → navigate to /board. | `PlanningChatContainer()` | NO production importer (Grep: only its own file + research/report .md). Renders `primitives/PlanningChat` (also no other importer). | **DEAD / superseded by CreateChatBoxContainer**. See candidates. |
| PreviewBrowserContainer.tsx | Dev-server preview iframe browser: start/stop, URL detect, responsive sizing. | `PreviewBrowserContainer({attemptId,className})` | Used by WorkspacesLayout (PREVIEW mode); usePreviewDevServer/usePreviewUrl, PreviewBrowser view. | Live. Invisible-ish (preview bridge, out of scope per R1). |
| PreviewControlsContainer.tsx | Dev-server controls + log tabs in right sidebar; hides when no dev script. | `PreviewControlsContainer(props)` | Used by RightSidebar (PREVIEW mode). | Live. |
| ProcessListContainer.tsx | List of execution processes with log search nav. | `ProcessListContainer()` | Used by RightSidebar (LOGS mode); ExecutionProcessesContext, LogsPanelContext, ProcessListItem, CollapsibleSectionHeader, InputField. | Live. |
| ProjectSelectorContainer.tsx | Searchable project dropdown (virtualized) with create-new. | `ProjectSelectorContainer(props)` | Verify importer (likely GitPanelCreate / settings). Dropdown primitive. | Live (presentational). |
| RecentReposListContainer.tsx | Lists recently-seen git repos; register on select. | `RecentReposListContainer(props)` | Used by `views/GitPanelCreate.tsx`; fileSystemApi.listGitRepos, repoApi.register. | Live. |
| RepoCard.tsx | Per-repo git card: branch/target dropdown, push button, PR status, actions, open-in-IDE/copy/settings menu. | `RepoCard(props)`, `RepoAction` type | Used by GitPanel view (RepoAction type imported by GitPanelContainer). | G2-CRITICAL. Live. "Open in IDE" dropdown item + `onOpenInEditor` prop (L248-250,68,93) is G1 deletion call-site. |
| ReviewCommentRenderer.tsx | Editable renderer for a user review comment. | `ReviewCommentRenderer({comment,projectId})` | Used by DiffViewCardWithComments; ReviewProvider, WYSIWYGEditor. | Live. |
| RightSidebar.tsx | Right sidebar router: concierge / create / changes / logs / preview branches; embeds GitPanelContainer + FeishuChannel. | `RightSidebar(props)`, `RightSidebarProps` | Used by WorkspacesLayout; composes GitPanelCreate/Feishu/FileTree/ProcessList/PreviewControls/GitPanel containers. | Live. |
| SearchableDropdownContainer.tsx | Generic searchable+virtualized dropdown logic, delegates to SearchableDropdown primitive. | `SearchableDropdownContainer<T>(props)` | Used by `primitives/RepoCardSimple.tsx` → SelectedReposList. | Live. |
| SessionChatBoxContainer.tsx | Custom-workspace session chat box: execution status, send/queue/edit, actions, model select, attachments. | `SessionChatBoxContainer(props)` | Verify importer (WorkspacesMain). Many hooks (useAttemptExecution, useSessionSend, etc.). | Live. Large (739L) — first 120 lines inspected (status compute + props + handlers). |
| SettingsLayoutContainer.tsx | Settings nav layout (general/projects/repos/agents/models/mcp/feishu/runtime/organizations). | `SettingsLayoutContainer()` | Settings route; SettingsLayout view, useUserSystem.remoteFeaturesEnabled. | Live. |
| VirtualizedProcessLogs.tsx | Virtuoso-based virtualized log renderer with search highlight. | `VirtualizedProcessLogs(props)`, `LogEntry`, `VirtualizedProcessLogsProps` | Used by LogsContentContainer. | Live. ~183L; first 40 inspected (types + scroll modifiers). |
| WorkflowProgressContainer.tsx | Live workflow progress (status/tasks/events) for materialized planning workflow. | `WorkflowProgressContainer({workflowId,onOpenBoard})` | Used by CreateChatBoxContainer; useWorkflowLiveStatus, WorkflowProgressView. | Live (System B tail). |
| WorkspacesLayout.tsx | Top-level orchestration workspace layout: mode routing (concierge/create/session), resizable panels, guide dialog, command bar. | `WorkspacesLayout()` | Route-level; composes Navbar/Sidebar/Main/RightSidebar/Changes/Logs/Preview + CreateChatBox/Concierge. | Live. Central orchestration entry. |
| WorkspacesMainContainer.tsx | Session-mode main area: task fetch, planning-message toggle, diff stats. | `WorkspacesMainContainer(props)` | Used by WorkspacesLayout; WorkspacesMain view, useWorkspacePlanningMessages. | Live. |
| WorkspacesSidebarContainer.tsx | Left sidebar: merges planning drafts + concierge sessions + active workspaces; route on click; delete concierge. | `WorkspacesSidebarContainer()` | Used by WorkspacesLayout; usePlanningDrafts, useConciergeSessions, conciergeApi. | Live. |

## Candidates summary
- **PlanningChatContainer.tsx** — DEAD (no production importer; superseded by CreateChatBoxContainer). Its view `primitives/PlanningChat` is dead too (same 0-importer result).
- **DiffViewCardWithComments / GitPanelContainer / RepoCard / ContextBarContainer / ChangesPanelContainer(attemptId)** — live, but each carries a G1 "open in external IDE/editor" call-site to be stripped (per docs/audit/R1).

## Invisible features
- Feishu sync (FeishuChannelContainer, CreateChatBoxContainer feishu toggle/history, ConciergeChatContainer) — bidirectional bot sync, not obvious UI.
- Concierge WebSocket (ConciergeChatContainer via conciergeWsStore).
- Preview dev-server iframe bridge (PreviewBrowser/PreviewControls) — out of scope per R1.
- Planning-draft System B confirm→materialize→workflow (CreateChatBoxContainer + AuditDocPanel + WorkflowProgressContainer).
- Open-in-IDE/editor (DiffViewCardWithComments, GitPanelContainer, RepoCard, ContextBarContainer) — G1 deletion target.
