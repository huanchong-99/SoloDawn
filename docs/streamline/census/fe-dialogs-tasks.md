# Census: frontend/src/components/dialogs/tasks/

Generated: 2026-06-14  
Branch: refactor/streamline-and-quality-gate-rules  
Unit: fe-dialogs-tasks (18 files)

## Module Map

| File | Purpose | Public Surface | Relations / Callers | Notes |
|------|---------|----------------|---------------------|-------|
| `ChangeTargetBranchDialog.tsx` | Branch picker for changing target branch on a workspace/attempt | `ChangeTargetBranchDialog` (NiceModal), `ChangeTargetBranchDialogProps`, `ChangeTargetBranchDialogResult` | Called by `GitOperations.tsx` (handleChangeTargetBranchDialogOpen); re-exported via `dialogs/index.ts` | Uses `BranchSelector` component |
| `CreateAttemptDialog.tsx` | Creates a new coding attempt, lets user pick executor profile + repo branches | `CreateAttemptDialog`, `CreateAttemptDialogProps` | Called by `NextActionCard.tsx`, `TaskPanel.tsx`, `actions-dropdown.tsx`; re-exported via `dialogs/index.ts` | Uses `useAttemptCreation`, `useRepoBranchSelection`, `useKeySubmitTask` |
| `CreatePRDialog.tsx` | Full PR creation form: title, body, base branch, draft toggle, auto-generate description; handles GH CLI setup errors | `CreatePRDialog`, `CreatePRDialogProps`, `CreatePRDialogResult` | Called by `GitOperations.tsx`, `ui-new/actions/index.ts`; re-exported via `dialogs/index.ts` | Mac-only GH CLI fallback path; delegates to `GhCliSetupDialog` |
| `DeleteTaskConfirmationDialog.tsx` | Destructive confirmation before deleting a task; calls `tasksApi.delete` directly | `DeleteTaskConfirmationDialog`, `DeleteTaskConfirmationDialogProps` | Called by `actions-dropdown.tsx`; registered in `types/modals.ts`; re-exported via `dialogs/index.ts` | Hardcoded English strings (no i18n); title/body/buttons not translated |
| `EditBranchNameDialog.tsx` | Rename the git branch for a workspace attempt | `EditBranchNameDialog`, `EditBranchNameDialogProps`, `EditBranchNameDialogResult` | Called by `actions-dropdown.tsx`; re-exported via `dialogs/index.ts` | Uses `useRenameBranch` mutation hook |
| `EditorSelectionDialog.tsx` | Fallback editor picker when default IDE open fails; lets user pick an `EditorType` | `EditorSelectionDialog`, `EditorSelectionDialogProps` | Imported by `useOpenInEditor.ts` (fallback path) and `ui-new/actions/index.ts` (fallback path). NOT exported via `dialogs/index.ts` | **G1 IDE deletion candidate** per task scope note. Used only as error fallback for "open in IDE" feature |
| `GitActionsDialog.tsx` | Full Git operations panel surfaced in a dialog (PR status, GitOperations toolbar) | `GitActionsDialog`, `GitActionsDialogProps` | Called by `NextActionCard.tsx`, `actions-dropdown.tsx`; re-exported via `dialogs/index.ts` | Wraps `GitOperations`, `ExecutionProcessesProvider`, `GitOperationsProvider` |
| `PrCommentsDialog.tsx` | Shows PR review comments; user can select subset to inject as follow-up context | `PrCommentsDialog`, `PrCommentsDialogProps`, `PrCommentsDialogResult` | Called by `TaskFollowUpSection.tsx`; NOT exported via `dialogs/index.ts` | Uses `usePrComments` hook; local `getErrorMessage` helper (not from `@/lib/modals`) |
| `ReassignDialog.tsx` | Reassign a shared task to another org member | `ReassignDialog`, `ReassignDialogProps` | Called by `actions-dropdown.tsx`; registered in `types/modals.ts`; re-exported via `dialogs/index.ts` | Requires `isCurrentAssignee` guard; uses `useProjectRemoteMembers` |
| `RebaseDialog.tsx` | Rebase dialog for tasks (old design): picks target branch + optional upstream | `RebaseDialog`, `RebaseDialogProps`, `RebaseDialogResult` | Called by `GitOperations.tsx`; re-exported via `dialogs/index.ts` | Parallel newer implementation exists at `ui-new/dialogs/RebaseDialog.tsx` (used by `ui-new/actions/index.ts`) |
| `RestoreLogsDialog.tsx` | Multi-repo worktree-reset confirmation before replaying execution logs from a past process | `RestoreLogsDialog`, `RestoreLogsDialogProps`, `RestoreLogsDialogResult` | Called by `useRetryProcess.ts`, `useMessageEditRetry.ts`; re-exported via `dialogs/index.ts` | Complex multi-repo state; fetches `executionProcessesApi.getRepoStates`; uses `useKeySubmitTask` |
| `ShareDialog.tsx` | Share a task to the remote (requires sign-in + project link); self-re-triggers via `OAuthDialog` | `ShareDialog`, `ShareDialogProps` | Called by `actions-dropdown.tsx`; registered in `types/modals.ts`; re-exported via `dialogs/index.ts` | Contains self-referential re-show pattern after OAuth; delegates to `LinkProjectDialog` |
| `StartReviewDialog.tsx` | Trigger a coding-agent code review session; picks agent/config, optional PR comments, optional new session | `StartReviewDialog`, `StartReviewDialogProps` | Called by `ui-new/actions/index.ts`; re-exported via `dialogs/index.ts` | Uses `sessionsApi.startReview`; integrates `AgentSelector`, `ConfigSelector` |
| `StopShareTaskDialog.tsx` | Confirmation to un-share a shared task | `StopShareTaskDialog`, `StopShareTaskDialogProps` | Called by `actions-dropdown.tsx`; registered in `types/modals.ts`; re-exported via `dialogs/index.ts` | State-machine enum for close outcome (replaces old dual-ref pattern) |
| `TagEditDialog.tsx` | Create or edit a reusable "Tag" (prompt snippet) in settings | `TagEditDialog`, `TagEditDialogProps`, `TagEditResult` | Called by `TagManager.tsx`; re-exported via `dialogs/index.ts` | Uses `settings` i18n namespace; tag.content required to enable Save |
| `TaskFormDialog.tsx` | Central task create/edit/duplicate/subtask form; WYSIWYG description, image upload, repo-branch selection, executor profile | `TaskFormDialog`, `TaskFormDialogProps` | Called via `openTaskForm` wrapper from `Navbar.tsx`, `actions-dropdown.tsx`, `ViewRelatedTasksDialog.tsx`; registered in `types/modals.ts`; re-exported via `dialogs/index.ts` | Most complex dialog; TanStack Form; keyboard shortcuts; unsaved-changes guard; drag-and-drop image |
| `ViewProcessesDialog.tsx` | Show execution process history in a full-height modal | `ViewProcessesDialog`, `ViewProcessesDialogProps` | Called by `actions-dropdown.tsx`, `NextActionCard.tsx`; re-exported via `dialogs/index.ts` | Thin wrapper around `ProcessesTab` + `ProcessSelectionProvider` |
| `ViewRelatedTasksDialog.tsx` | List parent/child tasks for an attempt; supports create-subtask inline | `ViewRelatedTasksDialog`, `ViewRelatedTasksDialogProps` | Called by `actions-dropdown.tsx`; re-exported via `dialogs/index.ts` | Invokes `openTaskForm` in subtask mode after closing |

## Candidates for Keep/Cut

| File | Kind | Disposition | Confidence | Notes |
|------|------|-------------|------------|-------|
| `EditorSelectionDialog.tsx` | dead/deprecated | delete | medium | Named IDE deletion target (G1). Used only as error-fallback in 2 places (`useOpenInEditor.ts` line 38, `ui-new/actions/index.ts` line 660). Both callers can be simplified to drop the fallback or re-route. |
| `RebaseDialog.tsx` (tasks/) | duplicate | investigate | medium | A newer `ui-new/dialogs/RebaseDialog.tsx` also exists and is used by the new `ui-new/actions` path. The old one is still called by `GitOperations.tsx`. Consolidation opportunity once old toolbar is removed. |
| `DeleteTaskConfirmationDialog.tsx` | bug | refactor | high | Title "Delete Task", body text, and button labels are hardcoded English — not wrapped in `useTranslation`. Every other dialog in this folder uses i18n. |
| `PrCommentsDialog.tsx` | dubious-feature | investigate | low | Not exported from `dialogs/index.ts`. Only caller is `TaskFollowUpSection.tsx`. The local `getErrorMessage` shadows the `@/lib/modals` helper of same name — minor encapsulation smell. |
