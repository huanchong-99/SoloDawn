# FE Hooks REST — Module Map

Census unit: `fe-hooks-rest`  
Scope: `frontend/src/hooks/` — all hooks not in fe-hooks-workflow / fe-hooks-session-conv, plus `hooks/auth/`

---

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|------|---------|---------------|-----------|-------|
| `auth/useAuth.ts` | Derive auth state from ConfigProvider loginStatus | `useAuth()` → `{isSignedIn, isLoaded, userId}` | Called by `useCurrentUser`, `useAuthStatus`, `useProjectTasks`, `useTaskMutations`, `usePreviousPath` | Core auth primitive |
| `auth/useAuthMutations.ts` | OAuth handoff init mutation | `useAuthMutations()` → `{initHandoff}` | Uses `oauthApi.handoffInit` | Login flow |
| `auth/useAuthStatus.ts` | Poll OAuth status, retry logic for 401/403 | `useAuthStatus(options)` → react-query result | Uses `useAuth`, `oauthApi.status`, refetchInterval=1s | Polling hook |
| `auth/useCurrentUser.ts` | Fetch current user, reset WS store on sign-out | `useCurrentUser()` → react-query result | Uses `useAuth`, `oauthApi.getCurrentUser`, `useWsStore` | WS cleanup on logout |
| `index.ts` | Barrel re-export | All named exports | Re-exports ~30 hooks + types | Does NOT export useQualityGate (uses named exports from it) |
| `useAgentAvailability.ts` | Check if a coding agent CLI is available | `useAgentAvailability(agent, options, refreshToken)` → state | Uses `configApi.checkAgentAvailability`, `useErrorNotification` | Polling; LOGIN_DETECTED/INSTALLATION_FOUND/NOT_FOUND |
| `useApprovalMutation.ts` | Approve or deny execution approval requests | `useApprovalMutation()` → `{approve, deny, isApproving, isDenying, ...}` | Uses `approvalsApi.respond`, invalidates executionProcesses/sessions | Human-in-loop approval |
| `useAssigneeUserName.ts` | Fetch assignee UserData for shared tasks | `useAssigneeUserNames({projectId, sharedTasks, enabled})` → `{assignees, refetchAssignees}` | Uses `getSharedTaskAssignees` remote API | Used by useProjectTasks |
| `useAttempt.ts` | Fetch a single workspace/attempt by ID | `useAttempt(attemptId, opts)` → `Query<Workspace>` | `attemptsApi.get`; provides `attemptKeys` | Simple fetch hook |
| `useAttemptBranch.ts` | Fetch the current branch of an attempt | `useAttemptBranch(attemptId)` → `{branch, isLoading, refetch}` | `attemptsApi.get` | Thin field extractor |
| `useAttemptConflicts.ts` | Abort conflicts for an attempt/repo | `useAttemptConflicts(attemptId, repoId)` → `{abortConflicts}` | `attemptsApi.abortConflicts` | Git conflict resolution |
| `useAttemptCreation.ts` | Create a new workspace/attempt for a task | `useAttemptCreation({taskId, onSuccess})` → `{createAttempt, isCreating, error}` | `attemptsApi.create`, invalidates `workspaceSummaryKeys` | Task start flow |
| `useAttemptExecution.ts` | Aggregate execution processes + stop action | `useAttemptExecution(attemptId, taskId)` → `{processes, isAttemptRunning, stopExecution, ...}` | Uses `ExecutionProcessesContext`, `useTaskStopping`, `executionProcessesApi.getDetails` | Core execution status |
| `useAttemptRepo.ts` | Fetch repos for an attempt, manage selection | `useAttemptRepo(attemptId, options)` → `{repos, selectedRepoId, setSelectedRepoId, ...}` | `attemptsApi.getRepos`, cache-based shared selection | Multi-repo selection |
| `useAutoLinkSharedTasks.ts` | Auto-link shared tasks assigned to current user | `useAutoLinkSharedTasks(props)` → void | Uses `useCurrentUser`, `useTaskMutations`; triggered by VITE_ENABLE_SHARED_TASKS flag | Behind feature flag (disabled server-side) |
| `useBranchStatus.ts` | Poll branch status for an attempt | `useBranchStatus(attemptId)` → react-query result | `attemptsApi.getBranchStatus`, refetchInterval=5s | Git status polling |
| `useChangeTargetBranch.ts` | Change the target branch for a repo in an attempt | `useChangeTargetBranch(attemptId, repoId, onSuccess, onError)` → mutation | `attemptsApi.change_target_branch`, invalidates branchStatus/taskAttempt/attemptRepo | Git operations |
| `useCliInstall.ts` | Install/uninstall a CLI type | `useCliInstall()` → `{installMutation, uninstallMutation}` | Fetch /api/cli-types/…/install, invalidates cliTypesKeys.detection | CLI management |
| `useCliInstallProgress.ts` | Stream install/uninstall progress via WS | `useCliInstallProgress(cliTypeId, jobId)` → `InstallProgressState` | Raw WebSocket to /api/cli-types/…/install/ws | CLI management, log streaming |
| `useCliTypes.ts` | Fetch CLI types, detect installations, fetch models | `useCliTypes()`, `useCliDetection()`, `useModelsForCli(cliTypeId)`, `cliTypesKeys` | Hits /api/cli_types and /api/cli_types/detect; provides `CliType`, `CliModel`, `CliDetectionResult` types | Central CLI registry |
| `useCommandBarShortcut.ts` | Attach Cmd/Ctrl+K keyboard shortcut | `useCommandBarShortcut(onOpen, enabled)` → void | DOM event listener via globalThis | Used by WorkspacesLayout |
| `useConcierge.ts` | CRUD + message ops for concierge sessions; Feishu channel | `useConciergeSessions`, `useConciergeSession`, `useConciergeMessages`, `useSendConciergeMessage`, `useCreateConciergeSession`, `useUpdateConciergeSettings`, `useFeishuChannel`, `useSwitchFeishuChannel`, `conciergeKeys` | Uses `conciergeApi`; Feishu integration | Feishu-connected assistant |
| `useContextBarPosition.ts` | Drag-to-snap position for floating context bar | `useContextBarPosition(containerRef, barWidth, barHeight)` → `{position, style, isDragging, dragHandlers}` | Uses `useContextBarPositionStore` from uiPreferencesStore | Used by ContextBar.tsx |
| `useConversationHistory.ts` | Stream + aggregate conversation entries per execution process | `useConversationHistory({attempt, onEntriesUpdated})` → `{}` | Uses `ExecutionProcessesContext`, `streamJsonPatchEntries`; WS streams /normalized-logs/ws & /raw-logs/ws | Core conversation engine |
| `useCreateAttachments.ts` | Upload images during task creation | `useCreateAttachments(onInsertMarkdown)` → `{uploadFiles, getImageIds, clearAttachments, localImages}` | `imagesApi.upload` | Task creation image handling |
| `useCreateModeState.ts` | Persistent draft state for task creation | `useCreateModeState({initialProjectId, initialRepos})` → full result set | Uses `useScratch`, `useDebouncedCallback`, `useProjects`, `useUserSystem`, `repoApi` | Draft persistence via scratch API |
| `useCreateSession.ts` | Create session + send first message | `useCreateSession()` → mutation | `sessionsApi.create` + `sessionsApi.followUp` | Session creation |
| `useCreateWorkspace.ts` | Create task+attempt and navigate | `useCreateWorkspace()` → `{createWorkspace}` | `tasksApi.createAndStart`, `attemptsApi.getAll`, navigate | Used in create flow |
| `useDebouncedCallback.ts` | Stable debounced callback with cancel | `useDebouncedCallback(callback, delay)` → `{debounced, cancel}` | Pure hook, no external deps | Utility; stable identity guarantee |
| `useDevServer.ts` | Start/stop dev server for an attempt | `useDevServer(attemptId, options)` → `{start, stop, isStarting, isStopping, runningDevServers, devServerProcesses}` | Uses `useAttemptExecution`, `attemptsApi.startDevServer`, `executionProcessesApi.stopExecutionProcess` | Dev server management |
| `useDevserverPreview.ts` | Derive dev server preview URL/status | `useDevserverPreview(attemptId, options)` → `DevserverPreviewState` | Uses `ExecutionProcessesContext` | Preview panel state |
| `useDevserverUrl.ts` | Detect devserver URL from log lines | `useDevserverUrlFromLogs(logs)` → `DevserverUrlInfo \| undefined`; `detectDevserverUrl(line)` (pure fn) | Uses `stripAnsi`; parses host:port and http/https URLs | Log parsing, URL detection |
| `useDiffStream.ts` | Stream file diffs via WS JSON patch | `useDiffStream(attemptId, enabled, options)` → `{diffs, error, isInitialized}` | Uses `useJsonPatchWsStream` | Diff view |
| `useDiffSummary.ts` | Aggregate diff stats (counts only) | `useDiffSummary(attemptId)` → `{fileCount, added, deleted, error}` | Uses `useDiffStream` with statsOnly=true | Stats display |
| `useEditorAvailability.ts` | Check if an editor (VS/cursor/etc.) is installed | `useEditorAvailability(editorType)` → `EditorAvailabilityState` | `configApi.checkEditorAvailability` | **G1 IDE-connect deletion target** |
| `useErrorNotification.ts` | Normalize + dispatch errors to optional handler | `useErrorNotification(options)` → `{notifyError, wrapAsync}` | No external deps; pure logic | Utility; widely used |
| `useExecutionProcesses.ts` | Stream execution processes for a session via WS | `useExecutionProcesses(sessionId, attemptId, opts)` → `{executionProcesses, executionProcessesById, isAttemptRunning, ...}` | Uses `useJsonPatchWsStream` over /execution-processes/stream/session/ws | Core streaming hook |
| `useExecutorSelection.ts` | Manage executor + variant selection priority | `useExecutorSelection({profiles, latestProfileId, isNewSessionMode, scratchVariant, configExecutorProfile})` → result | Uses `useVariant` | Executor/variant dropdown logic |
| `useFirstRun.ts` | Detect and complete first-run state | `useFirstRun()` → `{isFirstRun, completeFirstRun}` | Uses `useUserSystem`, `updateAndSaveConfig` | Windows installer first-run wizard |
| `useFollowUpSend.ts` | Send a follow-up message (legacy path) | `useFollowUpSend({sessionId, message, ...})` → `{isSendingFollowUp, followUpError, setFollowUpError, onSendFollowUp}` | `sessionsApi.followUp`, invalidates sessionMessages | Used in old follow-up path |
| `useForcePush.ts` | Force-push a branch | `useForcePush(attemptId, onSuccess, onError)` → mutation | `attemptsApi.forcePush`, invalidates branchStatus | Git operations |
| `useGitHubComments.ts` | Fetch + normalize GitHub PR review comments | `useGitHubComments({workspaceId, repoId, enabled})` → `{gitHubComments, getGitHubCommentsForFile, ...}` | Uses `usePrComments`, `usePersistedExpanded` | Diff view GitHub integration |
| `useGitOperations.ts` | Aggregate all git operations with error dispatch | `useGitOperations(attemptId, repoId)` → `{actions, isAnyLoading, states}` | Uses `useRebase`, `useMerge`, `usePush`, `useForcePush`, `useChangeTargetBranch`, `GitOperationsContext`, `ForcePushDialog` | Git operations facade |
| `useHasDevServerScript.ts` | Check if a project has a dev server script | `useHasDevServerScript(projectId)` → react-query boolean | `projectsApi.getRepositories` | Dev server feature gate |
| `useImageMetadata.ts` | Fetch image metadata from API or local cache | `useImageMetadata(taskAttemptId, src, taskId, localImages)` → `{data, isLoading}` | `/api/task-attempts/…/images/metadata` and `/api/images/task/…/metadata` | WYSIWYG image display |
| `useImageUpload.ts` | Upload images (global or per-task) | `useImageUpload()` → `{upload, uploadForTask, deleteImage}` | `imagesApi.upload/uploadForTask/delete` | Image management |
| `useJsonPatchWsStream.ts` | Generic WS JSON-Patch streaming with reconnect/backoff | `useJsonPatchWsStream<T>(endpoint, enabled, initialData, options)` → `{data, isConnected, isInitialized, error}` | Pure WS hook; used by useExecutionProcesses, useProjects, useProjectTasks, useScratch, useDiffStream | Core WS infrastructure |
| `useLogStream.ts` | Stream raw STDOUT/STDERR logs for a process | `useLogStream(processId)` → `{logs, error}` | WS /api/execution-processes/…/raw-logs/ws; 5000-entry cap | Log view |
| `useMerge.ts` | Merge attempt branch | `useMerge(attemptId, onSuccess, onError)` → mutation | `attemptsApi.merge`, invalidates branchStatus/repoBranches | Git operations |
| `useMessageEditRetry.ts` | Edit + retry a past message (with RestoreLogsDialog) | `useMessageEditRetry(sessionId, onSuccess, onError)` → mutation; exports `EditDialogCancelledError` | `sessionsApi.followUp` with retry_process_id; uses `RestoreLogsDialog` | Message edit path |
| `useModelConfigForExecutor.ts` | Get available model configs + manage selection | `useModelConfigForExecutor(executor, workflowModelLibrary)` → `{customModels, officialModels, allModels, selectedModelConfigId, ...}` | Uses `useModelsForCli` from useCliTypes | Workflow model selection |
| `useModelVerification.ts` | Verify model API key via backend | `useModelVerification()` → `{models, isLoading, isVerified, fetchModels, verifyModel, reset}` | `/api/models/list`, `/api/models/verify` | Setup wizard step 2 |
| `useNativeCredentials.ts` | Check Claude Code CLI OAuth credentials | `useNativeCredentials()` → `Query<NativeCredentialsStatus>` | `configApi.getNativeCredentialsStatus` | Native subscription detection |
| `useNavigateWithSearch.ts` | Navigate preserving URL search params | `useNavigateWithSearch()` → `NavigateFunction` | Wraps `useNavigate` + `useSearchParams` | Navigation utility |
| `useOpenInEditor.ts` | Open an attempt workspace in an external editor | `useOpenInEditor(attemptId, onShowEditorDialog)` → async callback | `attemptsApi.openEditor`, uses `EditorSelectionDialog` | **G1 IDE-connect deletion target** |
| `useOpenProjectInEditor.ts` | Open a project in an external editor | `useOpenProjectInEditor(project, onShowEditorDialog)` → async callback | `projectsApi.openEditor`, uses `ProjectEditorSelectionDialog` | **G1 IDE-connect deletion target** |
| `useOrganizationInvitations.ts` | Fetch pending org invitations (admin only) | `useOrganizationInvitations(options)` → `Query<Invitation[]>` | `organizationsApi.listInvitations`; gates on remoteFeaturesEnabled | Org management |
| `useOrganizationMembers.ts` | Fetch org members with profile | `useOrganizationMembers(organizationId)` → `Query<OrganizationMemberWithProfile[]>` | `organizationsApi.getMembers` | Org management |
| `useOrganizationMutations.ts` | Create/invite/remove/role-change/delete org operations | `useOrganizationMutations(options)` → `{createOrganization, createInvitation, revokeInvitation, removeMember, updateMemberRole, deleteOrganization, ...}` | `organizationsApi.*` | Org management CRUD |
| `useOrganizationProjects.ts` | Fetch remote projects for an org | `useOrganizationProjects(organizationId)` → `Query<RemoteProject[]>` | `organizationsApi.getProjects`; gates on remoteFeaturesEnabled | Org-project linking |
| `useOrganizationSelection.ts` | URL-synced org selection with defaults | `useOrganizationSelection({organizations, onSelectionChange})` → `{selectedOrgId, selectedOrg, handleOrgSelect}` | Uses `useSearchParams`; syncs orgId param | Settings page |
| `usePlanningDraft.ts` | Planning draft CRUD + messages + confirm + materialize + Feishu sync + AuditDoc | `usePlanningDrafts`, `usePlanningDraft`, `usePlanningDraftMessages`, `useSendPlanningMessage`, `useConfirmDraft`, `useUploadAuditDoc`, `useDeleteAuditDoc`, `useMaterializeDraft`, `useTogglePlanningFeishuSync`, `useWorkspacePlanningMessages`, `planningDraftKeys` | `planningDraftsApi`, `attemptsApi.getPlanningMessages`; invalidates workflowKeys | **System B AuditPlan** (confirm→materialize flow + Feishu) |
| `usePrComments.ts` | Fetch PR review comments for an attempt | `usePrComments(attemptId, repoId, opts)` → `Query<PrCommentsResponse>`; exports `prCommentsKeys` | `attemptsApi.getPrComments` | Diff GitHub integration |
| `usePreviewSettings.ts` | Per-workspace preview URL + screen size settings (persisted) | `usePreviewSettings(workspaceId)` → full result | Uses `useScratch`, `useDebouncedCallback` | Preview panel |
| `usePreviousPath.ts` | Track visited paths + navigate back skipping settings | `usePreviousPath()` → navigate callback; exports `resetPreviousPathHistory` | Uses `useAuth`, sessionStorage | Navigation utility |
| `useProfiles.ts` | Load/save agent profiles JSON file | `useProfiles()` → `{profilesContent, parsedProfiles, save, saveParsed, ...}` | `profilesApi.load/save` | Settings page |
| `useProjectMutations.ts` | Create/update/link/unlink local↔remote projects | `useProjectMutations(options)` → `{createProject, updateProject, linkToExisting, createAndLink, unlinkProject}` | `projectsApi.*`; invalidates projects/organization cache | Project management CRUD |
| `useProjectRemoteMembers.ts` | Fetch remote project members | `useProjectRemoteMembers(projectId)` → `Query<RemoteProjectMembersResponse>` | `projectsApi.getRemoteMembers`; gates on remoteFeaturesEnabled | Project sharing |
| `useProjectRepos.ts` | Fetch repos for a project | `useProjectRepos(projectId, opts)` → `Query<Repo[]>` | `projectsApi.getRepositories` | Repo management |
| `useProjectTasks.ts` | Stream local tasks via WS + Electric shared tasks | `useProjectTasks(projectId)` → `{tasks, tasksById, tasksByStatus, sharedTasksById, sharedOnlyByStatus, ...}` | Uses `useJsonPatchWsStream`, `useLiveQuery` (Electric), `useAssigneeUserNames`, `useAutoLinkSharedTasks` | Main task list feed |
| `useProjects.ts` | Stream all projects via WS JSON patch | `useProjects()` → `{projects, projectsById, isLoading, isConnected, error}` | Uses `useJsonPatchWsStream` over /api/projects/stream/ws | Core projects feed |
| `usePush.ts` | Push a branch to remote | `usePush(attemptId, onSuccess, onError)` → mutation | `attemptsApi.push`, invalidates branchStatus | Git operations |
| `useQualityGate.ts` | Fetch quality gate runs/details/issues; terminal latest | `useQualityRuns`, `useQualityRunDetail`, `useQualityIssues`, `useTerminalLatestQuality`, `qualityKeys` | Uses version-tolerant `qualityFetchSafe` wrapper; /api/workflows/…/quality endpoints | **System A Quality Gate** — exported from index.ts |
| `useRebase.ts` | Rebase an attempt's branch | `useRebase(attemptId, repoId, onSuccess, onError)` → mutation | `attemptsApi.rebase`, invalidates branchStatus/taskAttempt/repoBranches | Git operations |
| `useRenameBranch.ts` | Rename attempt branch (with optimistic update) | `useRenameBranch(attemptId, onSuccess, onError)` → mutation | `attemptsApi.renameBranch`, optimistic cache update | Git operations |
| `useRepoBranchSelection.ts` | Multi-repo branch picker | `useRepoBranchSelection({repos, initialBranch, enabled})` → `{configs, isLoading, setRepoBranch, getWorkspaceRepoInputs, reset}`; exports `RepoBranchConfig` | Uses `useRepoBranches` + `useQueries` | Workspace creation |
| `useRepoBranches.ts` | Fetch branches for one or multiple repos | `useRepoBranches(repoId, opts)`, `useMultiRepoBranches(repoIds)`, `repoBranchKeys` | `repoApi.getBranches` | Git branch dropdown |
| `useRetryProcess.ts` | Retry an existing execution process | `useRetryProcess(sessionId, onSuccess, onError)` → mutation; exports `RetryDialogCancelledError` | `sessionsApi.followUp` with retry_process_id; uses `RestoreLogsDialog` | Process retry path |
| `useScratch.ts` | Stream a scratch item via WS + CRUD | `useScratch(scratchType, id, options)` → `{scratch, isLoading, isConnected, updateScratch, deleteScratch}` | Uses `useJsonPatchWsStream`, `scratchApi` | Draft persistence infrastructure |
| `useScriptPlaceholders.ts` | Get OS-aware script placeholder text | `useScriptPlaceholders()` → `ScriptPlaceholders` | Uses `useUserSystem`, `createScriptPlaceholderStrategy` | Settings/script editor |
| `useSessionAttachments.ts` | Upload images in follow-up messages | `useSessionAttachments(workspaceId, onInsertMarkdown)` → `{uploadFiles, localImages, clearUploadedImages}` | `imagesApi.uploadForAttempt` | Follow-up image upload |
| `useSessionMessageEditor.ts` | Persistent draft for session follow-up messages | `useSessionMessageEditor({scratchId})` → `{localMessage, handleMessageChange, scratchData, clearDraft, ...}` | Uses `useScratch`, `useDebouncedCallback` | Follow-up editor state |
| `useSessionQueueInteraction.ts` | Queue a message for later execution | `useSessionQueueInteraction({sessionId})` → `{isQueued, queuedMessage, queueMessage, cancelQueue, refreshQueueStatus}` | `queueApi.getStatus/queue/cancel` | Queued message feature |
| `useSessionSend.ts` | Send in new or existing session | `useSessionSend({sessionId, workspaceId, isNewSessionMode, effectiveExecutor, ...})` → `{send, isSending, error, clearError}` | Uses `useCreateSession`, `sessionsApi.followUp` | Session chat send |
| `useSlashCommands.ts` | CRUD for slash command presets | `useSlashCommands`, `useSlashCommand`, `useCreateSlashCommand`, `useUpdateSlashCommand`, `useDeleteSlashCommand`, `slashCommandKeys` | /api/workflows/presets/commands | Workflow slash commands |
| `useTask.ts` | Fetch a single task | `useTask(taskId, opts)` → `Query<Task>`; exports `taskKeys` | `tasksApi.getById` | Core task data |
| `useTaskAttempt.ts` | Fetch workspace with/without session | `useTaskAttempt(attemptId)`, `useTaskAttemptWithSession(attemptId)` | `attemptsApi.get/getWithSession` | Workspace detail |
| `useTaskAttempts.ts` | Fetch all attempts for a task | `useTaskAttempts(taskId, opts)`, `useTaskAttemptsWithSessions(taskId, opts)`, `taskAttemptKeys` | `attemptsApi.getAll`, `sessionsApi.getByWorkspace` | Attempt list |
| `useTaskImages.ts` | Fetch images for a task | `useTaskImages(taskId)` → `Query<ImageResponse[]>` | `imagesApi.getTaskImages` | Task detail |
| `useTaskMutations.ts` | Create/update/delete/share/link tasks | `useTaskMutations(projectId)` → `{createTask, createAndStart, updateTask, deleteTask, shareTask, stopShareTask, linkSharedTaskToLocal}` | `tasksApi.*`, `useNavigateWithSearch`, auth guard | Core task mutations |
| `useTaskRelationships.ts` | Fetch parent/child task relationships | `useTaskRelationships(attemptId, opts)` → `Query<TaskRelationships>`; exports `taskRelationshipsKeys` | `attemptsApi.getChildren` | Subtask tree |
| `useTodos.ts` | Extract TodoManagement entries from conversation | `useTodos(entries)` → `{todos, inProgressTodo, lastUpdated}` | Uses `PatchTypeWithKey` from useConversationHistory | Todo panel |
| `useUserOrganizations.ts` | Fetch user's organizations | `useUserOrganizations()` → `Query<ListOrganizationsResponse>` | `organizationsApi.getUserOrganizations`; gates on login+remoteFeaturesEnabled | Org switcher |
| `useVariant.ts` | Manage variant selection priority (user > scratch > process) | `useVariant({processVariant, scratchVariant})` → `{selectedVariant, setSelectedVariant}` | Pure React state | Executor variant control |
| `useVideoProgress.ts` | Track HTML video element playback/buffer progress | `useVideoProgress(videoRef)` → `{isLoading, playedPercent, bufferedPercent, duration}` | DOM events on HTMLVideoElement | **0 callers in codebase** |
| `useWorkflowInvalidation.ts` | Subscribe to WS events and invalidate workflow cache | `useWorkflowInvalidation(workflowId)` → void | Uses `useWorkflowEvents` (wsStore), `workflowKeys` | Workflow real-time refresh |
| `useWorkflowLiveStatus.ts` | Combine WS events + persisted events for live timeline | `useWorkflowLiveStatus(workflowId)` → `{workflowStatus, tasks, events, isLoading, connectionStatus}` | Uses `useWorkflow`, `useWorkflowEvents`, /api/workflows/…/events | Workflow detail live panel |
| `useWorkflows.ts` | Full workflow CRUD + lifecycle mutations + orchestrator | `useWorkflows`, `useWorkflow`, `useCreateWorkflow`, `usePrepareWorkflow`, `useStartWorkflow`, `usePauseWorkflow`, `useStopWorkflow`, `useSubmitWorkflowPromptResponse`, `useSubmitOrchestratorChat`, `useOrchestratorMessages`, `useMergeWorkflow`, `useDeleteWorkflow`, `useUpdateTaskStatus`, `workflowKeys`, types | Internal `workflowsApi`; /api/workflows/* | **Central workflow engine** |
| `useWorkspaceSessions.ts` | Manage sessions list + selection for a workspace | `useWorkspaceSessions(workspaceId, options)` → `{sessions, selectedSession, selectedSessionId, selectSession, isNewSessionMode, startNewSession, ...}` | `sessionsApi.getByWorkspace` | Workspace session switcher |
