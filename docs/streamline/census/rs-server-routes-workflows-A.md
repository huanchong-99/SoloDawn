# Census: rs-server-routes-workflows-A

Unit scope: `crates/server/src/routes/workflows.rs` lines 1-2400 (first half of a 4713-line file).
The second half (2400-4713) is covered by a sibling unit; cross-references to symbols defined there are noted.

Tooling note: fast-context MCP returned `resource_exhausted` (quota) on every attempt (3 trace IDs). All cross-file usage verification fell back to Grep across the repo.

## Module map

| File (line range) | Purpose | Public surface | Relations | Notes |
|---|---|---|---|---|
| workflows.rs 1-56 | Imports + module header | none | Pulls in `db::models`, `services::orchestrator` (BusMessage, OrchestratorRuntime, TerminalCoordinator, status constants), `services::terminal` (TerminalLauncher), `CCSwitchService`, DTOs from `workflows_dto`, `crate::routes::terminals::start_terminal` | `start_terminal` import is used by runtime-terminal handler in 2nd half (2900s). `CreateTerminalRequest`/`CreateWorkflowTaskRequest` are `#[cfg(test)]`-only imports. |
| workflows.rs 62-131 | Request/Response DTOs | `pub struct WorkflowDetailResponse`, `WorkflowTaskDetailResponse`, `WorkflowCommandWithPreset`, `UpdateWorkflowStatusRequest`, `RecoveryResponse`, `CreateRuntimeTaskRequest`, `CreateRuntimeTerminalRequest`, `MergeWorkflowRequest` | Used by handlers in both halves | `WorkflowDetailResponse`/`WorkflowTaskDetailResponse`/`WorkflowCommandWithPreset` are `pub` but only flatten-serialize types; see candidates (possible unused vs. `WorkflowDetailDto`). |
| workflows.rs 133-177 | Status/mode constants + governance statics + secret regexes | `WORKFLOW_STATUSES`, `WORKFLOW_EXECUTION_MODES`, `MERGE_ALLOWED_*`, `RUNTIME_MUTABLE_*`, rate-limit/circuit constants, `OrchestratorGovernanceState` (private), `ORCHESTRATOR_GOVERNANCE_STATE` (Lazy Mutex), 2 secret regexes | Consumed by validators + orchestrator-chat handler (2nd half) | Governance state is process-global `Lazy<Mutex>`; TODO(G16-013) flags unbounded growth. |
| workflows.rs 184-221 | Router builder | `pub fn workflows_routes() -> Router<DeploymentImpl>` | Mounted by parent router module; wires 19 routes incl. orchestrator chat/messages, runtime task/terminal creation | `recover_workflows`, `stop_workflow`, runtime handlers etc. resolve to defs in 2nd half. |
| workflows.rs 223-307 | Status-transition + scope validators | `is_known_workflow_status`, `can_merge_from_workflow_status`, `default_runtime_terminal_auto_confirm` (serde default), `is_valid_workflow_status_transition`, `validate_workflow_status_transition`, `validate_task_workflow_scope`, `validate_runtime_mutation_workflow_status`, `has_configured_workflow_models` | All private; called by handlers in both halves | `can_merge_from_workflow_status` — verify caller (merge handler in 2nd half). |
| workflows.rs 309-382 | Orchestrator feature-flag + permission/redaction helpers | `is_orchestrator_chat_feature_enabled`, `redact_sensitive_content`, `digest_message`, `normalize_operator_id`, `extract_role_from_headers`, `ensure_orchestrator_permission` | All consumed by `submit_orchestrator_chat` / `list_orchestrator_messages` (lines 3228-3550, 2nd half) | env var compat: SOLODAWN_ vs GITCORTEX_ prefix shim. |
| workflows.rs 384-461 | Orchestrator governance: rate limit + circuit breaker | `enforce_orchestrator_rate_limit`, `ensure_orchestrator_circuit_closed`, `update_orchestrator_circuit_breaker` | Consumed by `submit_orchestrator_chat` (3312-3481) | Invisible feature: per-workflow circuit breaker (3 failures -> 120s cooldown) + 12 req/60s rate limit. |
| workflows.rs 463-495 | Orchestrator receipt/summary message builders | `build_orchestrator_receipt_message`, `build_orchestrator_summary_message` | Consumed by `submit_orchestrator_chat` (3460, 3475) | — |
| workflows.rs 497-625 | Broadcast + cascade finalization helpers | `broadcast_task_status`, `broadcast_runtime_terminal_status`, `cleanup_finished_workflow_logs_best_effort`, `cascade_finished_workflow_children` | Used by status/delete/pause/stop handlers + runtime task/terminal creation (2nd half) | TerminalLog cleanup + SQLite WAL checkpoint after finished-workflow log purge. |
| workflows.rs 627-631 | Auto-complete predicate | `should_auto_complete_workflow` | Called at 3102 (task-status handler, 2nd half) + 4 unit tests | — |
| workflows.rs 638-741 | Create-workflow request validation | `pub fn validate_create_request` | Called by `create_workflow` (903); `pub` | Validates diy vs agent_planned, tasks/terminals, merge/error terminal config, command JSON. |
| workflows.rs 746-856 | CLI/model config validation + inline auto-create | `validate_cli_and_model_configs` (private) | Called by `create_workflow` (916) | Auto-creates custom ModelConfig from inline data (`ModelConfig::create_custom`). |
| workflows.rs 858-894 | GET /api/workflows list | `list_workflows` handler | Uses `Workflow::find_by_project_with_counts` -> `WorkflowListItemDto` | — |
| workflows.rs 896-1218 | POST /api/workflows create | `create_workflow` handler | `validate_create_request`, `validate_cli_and_model_configs`, branch slug + git conflict check (`GitService::check_branch_exists` via spawn_blocking), `Workflow::create_with_tasks`, encrypts orchestrator + terminal API keys | G23-003 branch conflict; G01-002/003 transactional commands; vk_task_id FK-guard. |
| workflows.rs 1220-1340 | GET /api/workflows/:id detail + completion reconcile | `get_workflow`, `workflow_detail_needs_completion_reconciliation` | Calls `orchestrator_runtime().reconcile_workflow_completion`; uses status constants | Stale-running reconciliation: all tasks final + no active terminal -> reconcile. |
| workflows.rs 1342-1359 | DELETE /api/workflows/:id | `delete_workflow` | `stop_workflow_runtime_if_running` (2554), `cleanup_workflow_terminals` (2575), `Workflow::delete` | Cross-calls into 2nd-half helpers. |
| workflows.rs 1361-1400 | PUT /:id/status | `update_workflow_status` | `validate_workflow_status_transition`, `cascade_finished_workflow_children`, log cleanup | G16-012 blocks protected transitions (must use /prepare,/start,/merge). |
| workflows.rs 1402-1514 | Prepare rollback helper | `rollback_prepare_failure` | Kills terminals, unregisters prompt watcher + TerminalBridge (G02-001), resets terminal/process/session, sets workflow failed | Invisible: TerminalBridge unregister stops MessageBus->PTY forwarding. |
| workflows.rs 1516-1580 | Working-dir resolution | `resolve_workflow_working_dir` | project.default_agent_working_dir -> task worktree (`WorktreeManager::get_worktree_base_dir`) -> first repo path | G23-006 worktree isolation. |
| workflows.rs 1582-1655 | Prompt-watcher refresh | `refresh_prompt_watcher_registrations` | `prompt_watcher().register` per terminal w/ pty_session_id | Used by start_workflow (2047,2290). |
| workflows.rs 1657-1848 | POST /:id/prepare | `prepare_workflow` | CAS created/failed->starting, `TerminalCoordinator::with_message_bus`, `start_terminals_for_workflow_with_limit`, `TerminalLauncher::launch_all`, `Workflow::set_ready`, rollback on any failure | `configured_max_concurrent_terminals()` global cap; excess terminals stay queued. |
| workflows.rs 1850-2296 | POST /:id/start | `start_workflow` | Stale-running recovery (CAS->paused), self-heal re-prepare (recursively calls `prepare_workflow`), orchestrator vs DIY branch; DIY spawns instruction-dispatch + quiet-window monitor tokio tasks | TODO(G16-008) extract re-prepare. DIY quiet-window monitor = invisible background completion detector (60s silence). |
| workflows.rs 2298-2325 | auto_prepare_and_start | `pub enum AutoStartError`, `pub async fn auto_prepare_and_start` | Calls `prepare_workflow` + `start_workflow`; **called by `planning_drafts.rs:1017` (materialize_draft flow, System B)** | In-flight relevance: planning-draft confirm->materialize auto-launch path. |
| workflows.rs 2327-2388 | POST /:id/pause | `pause_workflow` | `stop_workflow_runtime_if_running`, `cleanup_workflow_terminals`, cascades running tasks->pending, terminals->not_started | — |
| workflows.rs 2390-2400 (continues past boundary) | POST /:id/resume (start) | `resume_workflow` (body extends into 2nd half) | Mirrors start_workflow self-heal; CAS paused->ready | Function spans scope boundary; body completes after line 2400. |

## In-flight work relevance
- (a) external IDE/editor "open in" feature [G1]: NOT present in this file's first half (grep clean for vscode/webview/open-editor).
- (b) VS Code webview bridge: NOT present here.
- (c) Quality Gate System A: no QualityGateConfig / quality-gate.yaml references in lines 1-2400.
- (d) planning-draft confirm->materialize + AuditPlan System B: `auto_prepare_and_start` (2307) is the auto-launch entrypoint invoked by `planning_drafts.rs:1017`. Workflow `audit_plan` field is initialized to `None` at creation (969).
