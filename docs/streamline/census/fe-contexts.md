# fe-contexts Census — frontend/src/contexts/ (19 files)

> Branch: refactor/streamline-quality-gates  
> Date: 2026-06-14  
> Tool note: fast-context MCP returned resource_exhausted on both calls; all cross-file usage verified via Grep.

## Module Map

| File | Purpose | Public Surface | Key Relations | Notes |
|---|---|---|---|---|
| `ActionsContext.tsx` | Centralized action execution hub; builds `ActionExecutorContext` from router, query-client, workspace, dev-server | `ActionsProvider`, `useActions` | consumes `WorkspaceContext`, `useDevServer`; consumed by `CommandBarDialog`, `NavbarContainer`, `SessionChatBoxContainer`, `GitPanelContainer`, `ContextBarContainer` | Active, core orchestration layer |
| `ApprovalFeedbackContext.tsx` | Modal-style "deny with reason" flow for human-approval gates; tracks timeout | `ApprovalFeedbackProvider`, `useApprovalFeedback`, `useApprovalFeedbackOptional` | wraps `useApprovalMutation`; consumed by `WorkspacesMain` (provider), `SessionChatBoxContainer` | Active; part of approval-gate system |
| `ApprovalFormContext.tsx` | Per-approval-id keyed form state (isEnteringReason, denyReason) shared across sibling components | `ApprovalFormProvider`, `useApprovalForm` | consumed by `ConversationListContainer`, `PendingApprovalEntry` | Active; complements ApprovalFeedbackContext |
| `ChangesViewContext.tsx` | Coordinates file selection between FileTree and ChangesPanelContainer; manages diff-path lookup | `ChangesViewProvider`, `useChangesView` | consumes `WorkspaceContext` (diffPaths), `useUiPreferencesStore`; consumed by `ChatMarkdown`, `NewDisplayConversationEntry`, `RightSidebar`, `WorkspacesLayout`, `FileTreeContainer`, `ChangesPanelContainer` | Active; bridges chat inline-code clicks to diff panel |
| `ClickedElementsProvider.tsx` | Accumulates click-to-component payloads from preview iframe bridge; generates markdown context for follow-up chat | `ClickedElementsProvider`, `useClickedElements`, `ClickedEntry` (type) | imports `OpenInEditorPayload` from `previewBridge.ts`; consumed by `App.tsx` (provider), `ClickedElementsBanner`, `TaskFollowUpSection` | Active; invisible feature — VS Code-style preview click-to-source feature; related to G1 "open in external editor" investigation |
| `CreateModeContext.tsx` | Holds draft state for new-workspace creation form (repos, branches, profile, message) | `CreateModeProvider`, `useCreateMode`, `useCreateModeOptional` | consumes `useCreateModeState`, `useWorkspaces`, `useTask`, `useAttemptRepo`; consumed by 7 containers including `WorkspacesLayout`, `CreateChatBoxContainer`, `PlanningChatContainer` | Active |
| `EntriesContext.tsx` | Simple list of `PatchTypeWithKey` conversation entries shared across sibling components | `EntriesProvider`, `useEntries` | consumed by `MessageEditContext` (internal dep), `WorkspacesMain`, `SessionChatBoxContainer`, `ConversationListContainer`, `TaskFollowUpSection`, `TaskAttemptPanel` | Active; used in both old and new design paths |
| `ExecutionProcessesContext.tsx` | Fetches and exposes execution processes (all + visible-filtered); derives running/loading/connected state | `ExecutionProcessesProvider`, `useExecutionProcessesContext` | wraps `useExecutionProcesses`; consumed by `RetryUiContext`, `NewDesignScope`, `ProcessListContainer`, `WorkspacesLayout`, `useActionVisibility`, `GitActionsDialog`, `useAttemptExecution`, `useConversationHistory`, `useDevserverPreview` | Active; central hub for process data |
| `GitOperationsContext.tsx` | Minimal error-state holder for git operations scoped to an attempt | `GitOperationsProvider`, `useGitOperationsError` | consumed by `useGitOperations` (hook), `RebaseDialog`, `ChangeTargetDialog`, `GitActionsDialog` | Active; thin wrapper |
| `LogsPanelContext.tsx` | Controls logs-panel content (process or tool-output), log search query, and match navigation | `LogsPanelProvider`, `useLogsPanel` | consumes `WorkspaceContext`, `useWorkspacePanelState`; consumed by `ChatScriptEntry`, `LogsContentContainer`, `NewDisplayConversationEntry`, `ProcessListContainer`, `RightSidebar`, `WorkspacesLayout` | Active |
| `MessageEditContext.tsx` | Tracks which conversation entry is being edited inline; grey-out logic for subsequent entries | `MessageEditProvider`, `useMessageEditContext` | depends on `EntriesContext`; consumed by `WorkspacesMain`, `NewDisplayConversationEntry`, `SessionChatBoxContainer` | Active; `useMessageEditContext` silently returns defaults when outside provider (safe fallback) |
| `PortalContainerContext.tsx` | Provides a `RefObject<HTMLElement>` to portal-based UI components (tooltips, dropdowns, dialogs) | `PortalContainerContext` (raw context), `usePortalContainer` | consumed by 12 files: `NewDesignScope`, `LegacyDesignScope`, `Tooltip`, `Dialog`, `Dropdown`, `WorkspacesGuideDialog`, and legacy UI primitives | Active; foundational utility |
| `ProcessSelectionContext.tsx` | Tracks selected process ID within a process-list view | `ProcessSelectionProvider`, `useProcessSelection` | consumed by `ViewProcessesDialog` (provider), `ProcessesTab` (consumer) | Active; scoped to legacy task-detail dialog |
| `ProjectContext.tsx` | Derives `projectId` from URL, fetches project data, manages page title | `ProjectProvider`, `useProject` | wraps `useProjects`; consumed by 13+ files including `TaskPanel`, `Navbar`, dialogs, `NormalizedConversation` components, and `App.tsx` | Active; high-impact central provider |
| `RetryUiContext.tsx` | Tracks which process is being retried; grey-out logic for subsequent processes | `RetryUiProvider`, `useRetryUi` | depends on `ExecutionProcessesContext`; consumed by `WorkspacesMain`, `ProcessesTab`, `TaskFollowUpSection`, `TaskAttemptPanel`, `UserMessage`, `DisplayConversationEntry` | Active; `useRetryUi` returns defaults silently when outside provider |
| `ReviewProvider.tsx` | Accumulates inline diff review comments and drafts; generates markdown summary | `ReviewProvider`, `useReview`, `useReviewOptional`, `ReviewComment` (type), `ReviewDraft` (type) | consumed by `WorkspacesLayout` (provider), `ReviewCommentRenderer`, `SessionChatBoxContainer`, `CommentWidgetLine`, `DiffViewCardWithComments`, `TaskFollowUpSection`, `DiffCard` | Active |
| `SearchContext.tsx` | Task-list search query state; scoped to `/projects/:id/tasks` routes | `SearchProvider`, `useSearch` | consumed by `App.tsx` (provider), `Navbar` (input UI) | Active; intentionally limited scope (tasks only) |
| `TabNavigationContext.tsx` | Context for tab-type navigation (logs/diffs/processes/preview); provides `TabNavContext`, `TabNavigationProvider`, `useTabNavigation` | `TabNavContext` (raw), `TabNavigationProvider`, `useTabNavigation` | `PendingApprovalEntry` consumes `TabNavContext` directly via `useContext`; **`TabNavigationProvider` is never mounted anywhere** | Suspect: Provider is dead-code. Consumer uses raw context with null-fallback (`tabNav ? tabNav.activeTab === 'logs' : true`), so it always behaves as if logs tab is active. Either the Provider was removed mid-refactor, or the feature was shelved. |
| `WorkspaceContext.tsx` | Root workspace context: current workspace data, sessions, repos, GitHub PR comments, diff stream | `WorkspaceProvider`, `useWorkspaceContext`, `useWorkspaceContextOptional`, `NormalizedGitHubComment` (re-export) | consumes `useWorkspaces`, `useAttempt`, `useWorkspaceSessions`, `useAttemptRepo`, `useGitHubComments`, `useDiffStream`; consumed by 19+ files; syncs diffPaths to `useDiffViewStore` | Active; most critical context in scope |

## Invisible Features

| Feature | Context File | What It Does | Seems Used | Note |
|---|---|---|---|---|
| Click-to-component / preview bridge | `ClickedElementsProvider.tsx` | Receives alt-click events from `click-to-react-component` library in preview iframe; stores component chain with source locations; generates markdown context snippet for follow-up messages | Yes — `ClickToComponent` in `main.tsx`, `ClickedElementsBanner` in `TaskFollowUpSection` | Related to G1 "open in external IDE" investigation. `SelectedComponent.editor` field in `previewBridge.ts` hints at editor-protocol launching. The context itself does NOT call the editor — it only accumulates click data for the AI chat context. |
| Approval timeout countdown | `ApprovalFeedbackContext.tsx` | Auto-sets `isTimedOut=true` via `setTimeout` based on server-provided `timeoutAt`; blocks submission after expiry | Yes | Background timer behavior, not surfaced in obvious UI |
| diffPaths store sync | `WorkspaceContext.tsx` | Syncs live diff file-path set into `useDiffViewStore` on every diff stream update; cleanup on unmount clears the store | Yes — comment W2-22-01/05 | Enables expand/collapse-all in DiffView without prop drilling |
| Page title management | `ProjectContext.tsx` | Sets `document.title` to project name or "SoloDawn" based on active project | Yes | Invisible browser-level UX |
| Tab navigation null-guard | `TabNavigationContext.tsx` | `PendingApprovalEntry` falls back to `isLogsTabActive=true` when context is null | Partially (provider never mounted) | The null-guard means the code runs but the tab-awareness feature is a no-op |
