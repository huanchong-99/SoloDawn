# Census: rs-services-orch-agent-A

**Unit scope:** `crates/services/src/services/orchestrator/agent.rs` lines **1–4100** (file total 12172 lines).
**Branch:** refactor/streamline-and-quality-gate-rules
**Tooling note:** fast-context MCP returned `resource_exhausted` on every attempt (trace IDs d18b78dd…, f4dc9173…). Fell back to Grep for all cross-file usage questions. Cross-file findings below are Grep-based.

This file is a single Rust module (`OrchestratorAgent` + free helpers). The "module map" is therefore a map of logical sections / method groups within the in-scope line range.

| Section (lines) | Purpose | Public surface | Relations | Notes |
|---|---|---|---|---|
| Imports + struct defs (1–117) | Module imports; `OrchestratorAgent` struct fields; private structs `InferredNoMetadataCompletion`, `QueuedTerminalDispatch`, `StallRecoveryTracker`; `TASK_HINT_FROM_COMMIT_RE` regex; module consts (`MAX_STALL_RECOVERY_ATTEMPTS`, `TERMINAL_INPUT_FAILURE_LIMIT`, `FINAL_REPAIR_TASK_NAME`, `FINAL_REPAIR_MAX_ROUNDS`). | `pub struct OrchestratorAgent` | Re-exported via `mod.rs` (`pub use agent::OrchestratorAgent`). Consumes `super::{config,constants,llm,message_bus,persistence,prompt_handler,runtime_actions,state,types}` + `concierge`, `error_handler`, `template_renderer`. | Regex `TASK_HINT_FROM_COMMIT_RE` used by `extract_task_hint_from_commit_message` (line 4556, out of scope). All consts/structs verified used (see candidates). |
| `StallRecoveryTracker` impl (119–153) | Cooldown + attempt tracking for stall recovery; force-complete after `MAX_STALL_RECOVERY_ATTEMPTS`. | private | Used by `recover_stalled_terminals` (1345). | — |
| Constructors + attach/restore (155–397) | `new` (LLM client w/ Claude native fallback to `claude-sonnet-4-6`), `with_llm_client` (test-only), `attach_runtime_actions/persistence/concierge_broadcaster`, `restore_state`, `maybe_save_state`, `runtime_actions`, `load_workflow`. | `pub fn new`, `pub fn with_llm_client(#[cfg(test)])`, `pub fn attach_*`, `pub async fn restore_state` | All called from `runtime.rs` production path (new/attach_*/restore_state at runtime.rs:385–406, 1152–1169); `with_llm_client` only from tests.rs/events_test.rs. | `new` hardcodes Claude fallback model `claude-sonnet-4-6` — relevant to Claude/Anthropic config. |
| Concierge bridge (296–341) | `push_concierge_terminal_bridge` — inserts system message + broadcasts to web WS + Feishu when session has `sync_terminal`/`feishu_sync`. | private | Calls `ConciergeSession::find_by_workflow_with_sync_terminal`, `ConciergeBroadcaster::broadcast`. Called at 2292 on terminal completion. | INVISIBLE FEATURE: Feishu/web terminal-bridge sync gated on per-session flags. |
| Workflow-mode / archetype detection (399–596) | `is_agent_planned_workflow`, `ensure_agent_planned_workflow`, `initialize_workflow_mode_state`, `detect_workflow_strategy`, `detect_workflow_archetype_from_dir`, `has_existing_project_files`, `scan_project_signal_files`, `is_project_signal_path`, `goal_suggests_hybrid_foundation`. | private | Drives `WorkflowStrategy`/`WorkflowArchetype` (Greenfield/ExistingCodebase/HybridFoundation). | Heuristic project-signal scan (depth 4, 600 nodes cap). `goal_suggests_hybrid_foundation` matches EN+ZH needles. |
| Agent-planned initial planning (598–887) | `run_initial_agent_planning_if_needed` (+ 2 retry paths), `build_initial_planning_prompt`, `build_agent_planned_context`. | private | Calls `call_llm`, `execute_instruction`; reads WorkflowTask/Terminal/CliType/ModelConfig/WorkflowCommand. Injects synthetic Claude native model when no configured models. | INVISIBLE FEATURE: synthetic `model-claude-sonnet` injected if native creds present. Heavily duplicated terminal-dispatch follow-up block (candidate, see below). |
| Task-state sync (889–959) | `task_planning_complete`, `sync_task_state_from_db`, `finalize_task_if_ready`. | private | `finalize_task_if_ready` called at 6058/6074 (out of scope). | — |
| Event loop (961–1134) | `run` (main select loop: bus recv + watchdog tick), `handle_message` (BusMessage dispatch). | `pub async fn run` | `run` invoked by `OrchestratorRuntime`. Watchdog drives dispatch_queued_terminals / recover_stalled_terminals / auto_complete_stalled_tasks / auto_sync_workflow_completion. | Out-of-order completion-like events explicitly dropped (M16). |
| Auto-complete / stall (1136–1552) | `auto_complete_stalled_tasks` (incl. R8-B3 enforce-deadlock + stuck-foundation synthetic replanning), `persist_event`, `recover_stalled_terminals`, `redispatch_stalled_terminal_instruction`. | private | Reads QualityRun blockers via `terminal_has_unresolved_enforce_blockers` (defined 9687, scope B). Uses `MAX_ENFORCE_DEADLOCK_BLOCKS`. | INVISIBLE FEATURE: stuck-foundation watchdog fabricates a synthetic `TerminalCompletionEvent` to force feature-task replanning. |
| Prompt-detected + finalize (1554–1640) | `handle_terminal_prompt_detected` (delegates to PromptHandler), `try_finalize_terminal` (CAS + fallback). | private | PromptHandler integration. | — |
| Workflow failure / cleanup (1642–1801) | `cleanup_finished_workflow_logs_best_effort`, `cascade_failed_workflow_children`, `mark_workflow_failed`, `clear_queued_terminal_dispatches`. | private | Cascades CANCELLED to tasks/terminals; checkpoints SQLite after log cleanup. | — |
| Terminal input delivery (1803–2067) | `handle_terminal_input_delivery_failure` (3-strike → fail workflow), `is_nonfatal_terminal_input_delivery_failure`, `is_post_gate_feedback_context`, `terminal_has_recorded_commit`, `publish_terminal_input_checked`. | private | Uses `TERMINAL_INPUT_FAILURE_LIMIT`, `GIT_COMMIT_METADATA_SEPARATOR`. | — |
| Terminal completion (2069–2437) | `handle_terminal_completed` — the central completion pipeline: triggers quality gate on every commit, quiet-window defer, R8-B3 guards, **acceptance-review gate call (2245)**, finalize, status publish, completion-LLM, next-terminal dispatch. | private (`async`) | Calls `run_acceptance_review_gate` (5080, scope B), `build_completion_prompt`, `execute_instruction`, `dispatch_next_terminal`, `auto_sync_workflow_completion`. | KEY integration point of System B (AuditPlan acceptance review) — call site only; impl is in scope B. |
| Checkpoint quality gate (2439–3067) | `handle_checkpoint_quality_gate` (Quality Gate System A: per-commit gate; off/shadow/warn/enforce; dedup/replay/idempotent; spawns 5-min-timeout engine future writing QualityRun + QualityIssueRecord), `is_checkpoint_duplicate`. | private | Calls `quality::engine::QualityEngine`, `WorktreeManager::get_worktree_base_dir`, `compute_merge_base`/`collect_changed_files_for_quality_gate`/`ensure_js_deps_installed_for_gate` (all scope B). Emits `QualityGateResultEvent`. | THREE-LAYER QUALITY GATE SYSTEM A core. Local `GateOutcome` struct + `skipped_outcome` fail-open helper inside spawned task. |
| Quality gate result handling (3069–3533) | `handle_quality_gate_result` — enforce/shadow/warn branching; metric-collection-failure vs environment-blocker vs real-blocker; R8 loop-progress classify + auto-pause; fix-prompt delivery (in-place PTY vs clean-context relaunch). | private | Uses `compute_blocker_multiset`, `classify_loop_progress`, `loop_progress_hint`, `LOOP_PAUSE_*` (scope B); `is_quality_run_only_infra_blockers`; `RuntimeActionService::has_recent_stop_hook`/`relaunch_terminal_clean_context`. | Re-enters completion via `handle_terminal_completed_skip_quiet_window`. |
| Quiet-window + skip variant (3535–3834) | `handle_terminal_completed_skip_quiet_window`, `ensure_task_state_initialized_for_completion`, `defer_terminal_completion` (spawned poll loop), `remaining_terminal_quiet_duration[_from_db]`. | private | DB-driven last-activity query on `terminal_log`/`terminal`. | — |
| Dispatch plumbing (3836–4100) | `enqueue_terminal_dispatch`, `task_status_blocks_dispatch`, `workflow_status_is_finished`, `task_and_workflow_allow_dispatch`, `abort_dispatch_after_cas`, `dispatch_terminal_when_ready_or_queue`, `dispatch_queued_terminals`. | private | Global launch-slot queue (`queued_terminal_dispatches`). Calls `runtime_actions().try_start_terminal`, `dispatch_terminal` (6633, scope B). | — |

## In-flight-work relevance summary
- **(a) Open in external IDE/editor [G1]:** NOT present anywhere in lines 1–4100. No `editor`/`ide`/`external`/`vscode` symbols.
- **(b) VS Code webview bridge:** NOT present in scope. (The only "bridge" here is the concierge terminal text bridge → web WS / Feishu.)
- **(c) Quality Gate System A (quality/quality-gate.yaml, QualityGateConfig):** CORE of this scope. Implemented in `handle_checkpoint_quality_gate` (2451) + `handle_quality_gate_result` (3075). `QualityGateConfig` itself is not referenced by name here; config arrives via `self.config.quality_gate_mode` (off/shadow/warn/enforce) and the engine loads `quality/quality-gate.yaml` inside `QualityEngine::from_project`.
- **(d) Planning-draft confirm→materialize + AuditPlan System B:** call site only. `run_acceptance_review_gate` is invoked at line 2245; the impl (`run_acceptance_review`, `AuditPlan` deserialize from `workflow.audit_plan`, scoring prompt) lives at 5080–5459 (scope B). No planning-draft `materialize`/`confirm` flow code in this range (the `auto_confirm` fields at 5984+ are for create_terminal/start_terminal instructions, out of scope and unrelated to planning-draft).

## Out-of-scope dependencies consumed by this scope (defined in scope B, agent.rs >4100)
`run_acceptance_review_gate` (5080), `terminal_has_unresolved_enforce_blockers` (9687), `is_quality_run_only_infra_blockers` (9650), `compute_blocker_multiset` (9809), `classify_loop_progress` (9875), `loop_progress_hint` (9984), `LoopProgressClass`/`LOOP_PAUSE_*` (9835/9866/9867), `TerminalGateChangedFiles` (9380), `collect_changed_files_for_quality_gate` (9423), `ensure_js_deps_installed_for_gate` (9529), `compute_merge_base` (10157), `dispatch_terminal` (6633), `build_completion_prompt` (4988), `build_task_instruction` (7296), `needs_explicit_submit`/`stall_recovery_submit_keystroke_schedule_ms` (7171/7200), `enforce_terminal_completion_shutdown` (7005).
