# Census: fe-workflow-wizard

Unit: `frontend/src/components/workflow/` — 7-step DIY workflow creation wizard

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|------|---------|----------------|-----------|-------|
| `types.ts` | Core domain types + API transformation | `WizardStep`, `WizardConfig`, `ModelConfig`, `TaskConfig`, `TerminalConfig`, `CommandConfig`, `AdvancedConfig`, `BasicConfig`, `ProjectConfig`, `GitStatus`, `WorkflowExecutionMode`, `ApiType`, `WizardStepMeta`, `WIZARD_STEPS`, `NATIVE_MODEL_ID`, `getDefaultWizardConfig()`, `isAgentPlannedMode()`, `getVisibleWizardSteps()`, `getVisibleWizardStepIds()`, `createNativeModelConfig()`, `wizardConfigToCreateRequest()` | Imported by: `WorkflowWizard.tsx`, all steps, all validators, `hooks/`, `stores/modelStore.ts`, `stores/wizardStore.ts`, `pages/Workflows.tsx`, `pages/ui-new/settings/ModelsSettingsNew.tsx`, `pages/SlashCommands.e2e.test.tsx`, `components/setup/SetupWizardStep2ModelContainer.tsx`, `components/ui-new/containers/CreateChatBoxContainer.tsx`, `hooks/useModelVerification.ts`, `hooks/useModelConfigForExecutor.ts`, `components/ui-new/containers/PlanningChatContainer.tsx` | Hub type file; `wizardConfigToCreateRequest` is complex transform used once (in `pages/Workflows.tsx`). `kanbanTaskIds` field defined but never read. |
| `constants.ts` | CLI type registry + git commit templates | `CLI_TYPES`, `CliTypeId`, `GIT_COMMIT_FORMAT`, `GIT_COMMIT_TYPES` | Imported by: `TerminalCard.tsx`, `steps/Step3Models.tsx`, `steps/Step6Advanced.tsx`, `components/setup/SetupWizardStep2Model.tsx` | `GIT_COMMIT_TYPES` object has ZERO imports anywhere — unused export. `GIT_COMMIT_FORMAT` only used in Step6Advanced as a display-only preview. |
| `WorkflowWizard.tsx` | Main 7-step wizard container; orchestrates navigation, validation, state, model library persistence | `WorkflowWizard` (default export) | Used by: `pages/Workflows.tsx`. Consumes: `StepIndicator`, all step components, `useWizardNavigation`, `useWizardValidation`, `validators/index`, `useUserSystem`, `useNativeCredentials` | All wizard state lives here. `persistWorkflowModelLibrary` persists non-native models to user config. G25-015 comment: auto-initialize tasks/terminals on diy←agent_planned mode switch. |
| `StepIndicator.tsx` | Progress bar showing current/completed steps with connector lines and checkmarks | `StepIndicator` | Used by: `WorkflowWizard.tsx` only | Purely presentational. Adapts to `visibleSteps` list for agent_planned mode (fewer steps). |
| `PipelineView.tsx` | Runtime workflow pipeline view (NOT part of wizard creation flow) — shows tasks, terminals, status, quality badges | `PipelineView`, `WorkflowStatus`, `WorkflowTask`, `MergeTerminal`, `TerminalQualityIndicator` (internal) | Used by: `pages/Workflows.tsx`, `pages/WorkflowDebugPage.tsx`. Consumes: `TerminalCard`, `QualityBadge`, `useTerminalLatestQuality` | This is a RUNTIME view, not a wizard step. Lives in the same folder but serves a different concern. Imports quality gate hook — links to Quality Gate System. |
| `QualityBadge.tsx` | Quality gate status badge using StatusPill primitive | `QualityBadge`, `GateStatus` | Used by: `PipelineView.tsx` (internal), `components/terminal/TerminalDebugView.tsx`, `components/quality/QualityReportPanel.tsx`. Prop `mode` defined but unused in render logic | `totalIssues` prop received but not rendered in the component body (only `blockingIssues` is used in label). Relevant to Quality Gate System C. |
| `TerminalCard.tsx` | Runtime terminal status card (clickable) | `TerminalCard`, `Terminal`, `TerminalStatus` | Used by: `PipelineView.tsx`, `pages/WorkflowDebugPage.tsx`, `components/terminal/TerminalDebugView.tsx`. `TerminalStatus` type imported by `utils/terminalStatus.ts` | Stateless; status drives visual style. |
| `WorkflowPromptDialog.tsx` | Modal dialog for interactive terminal prompts (yes/no, choice, arrow-select, input, password, enter-confirm) | `WorkflowPromptDialog`, `ENTER_CONFIRM_RESPONSE_TOKEN` | Used by: `pages/Workflows.tsx` (token also used there for enter-confirm response routing) | Complex prompt routing. `ENTER_CONFIRM_RESPONSE_TOKEN` is a sentinel string imported by Workflows.tsx to distinguish enter-confirm responses from regular input. Not related to wizard creation. |
| `steps/index.ts` | Re-exports all 7 step components | (re-exports only) | Used by: `WorkflowWizard.tsx` | Comment "Placeholder exports" is stale — all steps are fully implemented. |
| `steps/Step0Project.tsx` | Step 0: Working directory selection + git status check | `Step0Project` | Consumed by `WorkflowWizard.tsx`. Calls `/api/git/status` and `/api/git/init`. Uses `useProjectRepos`, `FolderPickerDialog`, `useErrorNotification`, `useUserSystem` | Auto-selects single project repo. Container-aware: shows hint when `is_containerized`. |
| `steps/Step1Basic.tsx` | Step 1: Workflow name, description, execution mode, task count, kanban import toggle | `Step1Basic` | Consumed by `WorkflowWizard.tsx` | `importFromKanban` toggle exists in UI but `kanbanTaskIds` are never populated downstream — feature is stub/incomplete. Task count options hardcoded to [1,2,3,4] + custom 5-10. |
| `steps/Step2Tasks.tsx` | Step 2: Per-task name/description/branch/terminal-count config with carousel navigation | `Step2Tasks` | Consumed by `WorkflowWizard.tsx`. Skipped in agent_planned mode. | `slugify()` auto-fills branch from task name. Carousel allows editing up to 10 tasks. Skipped by `getVisibleWizardStepIds` when `executionMode === 'agent_planned'`. |
| `steps/Step3Models.tsx` | Step 3: AI model CRUD — add/edit/delete, native subscription detection, API key verify, model list fetch | `Step3Models` | Consumed by `WorkflowWizard.tsx` AND `pages/ui-new/settings/ModelsSettingsNew.tsx` (reused via `dialogContentClassName` prop). Uses `useModelStore`, `useNativeCredentials`, `useToast`, `ConfirmDialog` | `dialogContentClassName` prop enables embedding in settings page. Called `useModelStore.getState()` inside event handlers (not a hook violation but non-idiomatic). |
| `steps/Step4Terminals.tsx` | Step 4: Assign CLI + model per terminal per task. Calls `/api/cli_types/detect`. Contains legacy CLI response adapter. | `Step4Terminals` | Consumed by `WorkflowWizard.tsx`. Skipped in agent_planned mode. | `LEGACY_CLI_DISPLAY_NAMES` + `LEGACY_CLI_ID_ALIASES` are backwards-compat shims for old API response format (map vs array). Normalizes terminals on mount to match task.terminalCount. `getCompatibleModelIds` callback duplicated from Step6Advanced logic — potential refactor target. |
| `steps/Step5Commands.tsx` | Step 5: Slash command preset selection, ordering, per-preset description/command/JSON-params editor | `Step5Commands`, `SYSTEM_PRESETS` | Consumed by `WorkflowWizard.tsx`. Calls `/api/workflows/presets/commands` for user presets. | `PresetEditorModal` is an inline component (not extracted). `SYSTEM_PRESETS` exported — currently only imported within same file (test in SlashCommands.e2e.test.tsx also imports it). |
| `steps/Step6Advanced.tsx` | Step 6: Orchestrator model, error terminal, merge terminal, git watcher, target branch, git commit format preview | `Step6Advanced` | Consumed by `WorkflowWizard.tsx`. Uses `CollapsibleSection`, `CLI_TYPES`, `GIT_COMMIT_FORMAT`. | `getModelsForCli` and `isModelCompatibleWithCli` logic is DUPLICATED in `validators/step6Advanced.ts` and `types.ts` — same pattern repeated in 4 places. `CollapsibleSection persistKey="wizard-git-commit-format"` — persists collapse state. |
| `hooks/useWizardNavigation.ts` | Step navigation hook — ordered step list, next/previous/goToStep | `useWizardNavigation`, `UseWizardNavigationOptions`, `UseWizardNavigationReturn` | Used by: `WorkflowWizard.tsx` only (production); tests in `hooks/__tests__/` | `isStepValid` option (E11-10) defined in interface but NEVER passed by `WorkflowWizard.tsx` — forward-jump gating is dead code path. `goToStep` returned from hook but not used in `WorkflowWizard.tsx`. |
| `hooks/useWizardValidation.ts` | Per-step validation state hook | `useWizardValidation`, `UseWizardValidationReturn` | Used by: `WorkflowWizard.tsx` only. Calls `validateWizardStep` from validators. | Simple state wrapper; `hasErrors` derived from `errors` but not used in `WorkflowWizard.tsx`. |
| `validators/index.ts` | Validator dispatch: `validateWizardStep` (single step) + `validateAllWizardSteps` (all visible steps) | `validateWizardStep`, `validateAllWizardSteps`, re-exports all per-step validators | Used by: `hooks/useWizardValidation.ts`, `WorkflowWizard.tsx` (directly imports `validateAllWizardSteps`) | Individual validators also re-exported but only consumed via this aggregator in production. |
| `validators/step0Project.ts` | Validates working directory presence and git repo status | `validateStep0Project` | Used via `validators/index.ts` | |
| `validators/step1Basic.ts` | Validates name required, initialGoal when agent_planned, taskCount 1-10 | `validateStep1Basic` | Used via `validators/index.ts` | |
| `validators/step2Tasks.ts` | Validates each task name/description/branch, duplicate branch detection | `validateStep2Tasks` | Used via `validators/index.ts` | No-op for agent_planned mode. |
| `validators/step3Models.ts` | Validates models present; for non-native models checks cliTypeId/apiKey/baseUrl/modelId | `validateStep3Models` | Used via `validators/index.ts` | |
| `validators/step4Terminals.ts` | Validates terminals present, each has valid taskId/cliTypeId/modelConfigId, checks model-CLI compatibility | `validateStep4Terminals` | Used via `validators/index.ts` | No-op for agent_planned mode. |
| `validators/step5Commands.ts` | Validates that if commands enabled, at least one preset selected | `validateStep5Commands` | Used via `validators/index.ts` | Simplest validator. |
| `validators/step6Advanced.ts` | Validates orchestrator model, error terminal (optional), merge terminal CLI+model, checks model-CLI compat | `validateStep6Advanced` | Used via `validators/index.ts` | Contains `isModelCompatibleWithCli` function — 4th copy of same logic across the codebase. |
| `types.test.ts` | Tests for types module: enum values, WIZARD_STEPS, getDefaultWizardConfig, wizardConfigToCreateRequest | — | Test file only | |
| `WorkflowWizard.test.tsx` | Integration tests for wizard component | — | Test file only | |
| `WorkflowPromptDialog.test.tsx` | Tests all prompt dialog modes | — | Test file only | |
| `StepIndicator.test.tsx` | Tests step indicator rendering | — | Test file only | |
| `PipelineView.test.tsx` | Tests pipeline view rendering | — | Test file only | |
| `TerminalCard.test.tsx` | Tests terminal card status rendering | — | Test file only | |
| `__tests__/QualityBadge.test.tsx` | Tests all QualityBadge status cases | — | Test file only | |
| `steps/Step0Project.test.tsx` | Tests folder selection, git status flow | — | Test file only | |
| `steps/Step1Basic.test.tsx` | Tests step 1 form interactions | — | Test file only | |
| `steps/Step2Tasks.test.tsx` | Tests task carousel and branch slugify | — | Test file only | |
| `steps/Step3Models.test.tsx` | Tests model CRUD flow | — | Test file only | |
| `steps/Step4Terminals.test.tsx` | Tests terminal assignment flow | — | Test file only | |
| `steps/Step5Commands.test.tsx` | Tests command preset selection | — | Test file only | |
| `steps/Step6Advanced.test.tsx` | Tests advanced config fields | — | Test file only | |
| `hooks/__tests__/useWizardNavigation.test.ts` | Tests navigation hook | — | Test file only | |
| `hooks/__tests__/useWizardValidation.test.ts` | Tests validation hook | — | Test file only | |
| `validators/__tests__/step4Terminals.test.ts` | Tests terminal validator | — | Test file only | |
| `validators/__tests__/steps.test.ts` | Tests all step validators | — | Test file only | |
