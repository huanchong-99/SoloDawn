# Census: fe-uinew-dialogs-actions

**Branch**: refactor/streamline-quality-gates  
**Date**: 2026-06-14  
**Scope**: `frontend/src/components/ui-new/` — `dialogs/`, `dialogs/commandBar/`, `actions/`, `hooks/`, `scope/`, `utils/`, `types/`

---

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|------|---------|----------------|-----------|-------|
| `actions/index.ts` | Central action registry: defines all app actions (workspace CRUD, layout toggles, git ops, dev server, IDE open), helper types, navbar/context-bar groups | `Actions` (const record), `ActionDefinition`, `GlobalActionDefinition`, `WorkspaceActionDefinition`, `GitActionDefinition`, `ActionVisibilityContext`, `ActionExecutorContext`, `SpecialIconType`, `ActionIcon`, `DevServerState`, `NavbarActionGroups`, `ContextBarActionGroups`, `NavbarDivider`, `ContextBarDivider`, `NavbarItem`, `ContextBarItem`, `resolveLabel`, `isSpecialIcon` | Imported by: `actions/pages.ts`, `actions/useActionVisibility.ts`, `dialogs/CommandBarDialog.tsx`, `contexts/ActionsContext.tsx`, `ui-new/containers/*`, `ui-new/views/Navbar.tsx`, `ui-new/primitives/ContextBar.tsx` | G1 deletion candidate: `OpenInIDE` action uses `ide-icon` special type; `ContextBarActionGroups.primary` includes it. `OpenInOldUI` action navigates to classic UI — legacy escape hatch. |
| `actions/pages.ts` | Defines command-bar page hierarchy (root, workspaceActions, diffOptions, viewOptions, gitActions) and helper `getPageActions` | `Pages`, `PageId`, `StaticPageId`, `CommandBarPage`, `CommandBarGroup`, `CommandBarGroupItem`, `CommandBarItem`, `RepoItem`, `ResolvedGroupItem`, `ResolvedGroup`, `getPageActions` | Imports `Actions` from `index.ts`; imported by `CommandBarDialog.tsx`, `commandBar/injectSearchMatches.ts`, `commandBar/useResolvedPage.ts`, `commandBar/useCommandBarState.ts`, `contexts/ActionsContext.tsx` | Contains reference to `Actions.OpenInIDE` in root page — affected by G1 deletion |
| `actions/useActionVisibility.ts` | Hook + pure helpers for evaluating action/page visibility, active state, enabled state, icon, tooltip, label from stores | `useActionVisibilityContext`, `isActionVisible`, `filterVisibleActionItems`, `isPageVisible`, `isActionActive`, `isActionEnabled`, `getActionIcon`, `getActionTooltip`, `getActionLabel` | Imported by: `CommandBarDialog.tsx`, `commandBar/injectSearchMatches.ts`, `commandBar/useResolvedPage.ts`, `contexts/ActionsContext.tsx`, `containers/NavbarContainer.tsx`, `containers/ContextBarContainer.tsx`, `containers/SessionChatBoxContainer.tsx`, `views/Navbar.tsx`, `primitives/ContextBar.tsx`, `primitives/SessionChatBox.tsx` | `filterVisibleActionItems` exported but has **zero callers** in production code (dead export). |
| `dialogs/ChangeTargetDialog.tsx` | Modal for changing a repo's target branch; wraps `GitOperationsProvider` + `useGitOperations` | `ChangeTargetDialog` (NiceModal), `ChangeTargetDialogProps` | Called by `Actions.GitChangeTarget.execute` in `actions/index.ts` | Fully active |
| `dialogs/CommandBarDialog.tsx` | Full-featured command palette dialog; wires state machine, resolved page, action execution | `CommandBarDialog` (NiceModal), `CommandBarDialogProps` | Imported dynamically by `Actions.OpenCommandBar.execute`; uses `useCommandBarState`, `useResolvedPage`, `useActionVisibilityContext` | VS Code-style command bar; active production UI |
| `dialogs/ConfirmDialog.tsx` | Generic imperative confirm modal (title, message, variant) | `ConfirmDialog` (NiceModal), `ConfirmDialogProps` | Called by `Actions.DeleteWorkspace`, `Actions.GitMerge` (rebase prompt + merge confirm) in `actions/index.ts` | Fully active |
| `dialogs/CreateProjectDialog.tsx` | Modal to create a new project (name only) | `CreateProjectDialog` (NiceModal), `CreateProjectDialogProps`, `CreateProjectDialogResult` | Imported by `pages/Workflows.tsx`, `pages/ui-new/settings/ProjectSettingsNew.tsx`, `ui-new/containers/GitPanelCreateContainer.tsx` | Fully active |
| `dialogs/CreateRepoDialog.tsx` | Modal to initialise a new git repo (name + location, folder picker) | `CreateRepoDialog` (NiceModal), `CreateRepoResult` | Imported by `ui-new/containers/CreateRepoButtonContainer.tsx` | Fully active |
| `dialogs/RebaseDialog.tsx` | Modal for rebasing with branch selection + advanced upstream option; chains to `ResolveConflictsDialog` on conflict | `RebaseDialog` (NiceModal), `RebaseDialogProps` | Called by `Actions.GitRebase.execute` and `Actions.GitMerge.execute` in `actions/index.ts` | Fully active |
| `dialogs/RenameWorkspaceDialog.tsx` | Modal for renaming a workspace; mutates via `attemptsApi.update` | `RenameWorkspaceDialog` (NiceModal), `RenameWorkspaceDialogProps`, `RenameWorkspaceDialogResult` | Called by `Actions.RenameWorkspace.execute` in `actions/index.ts` | Fully active |
| `dialogs/ResolveConflictsDialog.tsx` | Modal to resolve merge/rebase conflicts by dispatching an agent session with auto-generated instructions; supports new vs existing session | `ResolveConflictsDialog` (NiceModal), `ResolveConflictsDialogProps`, `ResolveConflictsDialogResult` | Called by `Actions.GitMerge.execute`, `Actions.GitRebase.execute`, `RebaseDialog.tsx` | Fully active; complex |
| `dialogs/WorkspacesGuideDialog.tsx` | Multi-topic onboarding guide panel (9 topics with images) rendered via `createPortal` | `WorkspacesGuideDialog` (NiceModal) | Called by `Actions.WorkspacesGuide.execute` in `actions/index.ts`; uses `PortalContainerContext` | New-design-specific guide; uses portal rendering |
| `dialogs/commandBar/injectSearchMatches.ts` | Pure utility: search filter across injectable pages for command bar root search | `injectSearchMatches` | Called by `commandBar/useResolvedPage.ts` | Internal to command bar system |
| `dialogs/commandBar/useCommandBarState.ts` | State machine (reducer) for command bar: browsing, selectingRepo, navigation stack | `useCommandBarState`, `CommandBarState`, `CommandBarEvent`, `CommandBarEffect` | Called by `CommandBarDialog.tsx` | Clean finite-state machine design |
| `dialogs/commandBar/useResolvedPage.ts` | Hook that expands page definition + search results into renderable groups | `useResolvedPage`, `ResolvedCommandBarPage` | Called by `CommandBarDialog.tsx`; internally calls `injectSearchMatches` | PAGE_ICONS map defines icons per static page |
| `hooks/usePreviewDevServer.ts` | Mutations for start/stop dev server with cache invalidation | `usePreviewDevServer` | Imported by `containers/PreviewBrowserContainer.tsx`, `containers/PreviewControlsContainer.tsx` | Duplicate path to `hooks/useDevServer.ts` (global hook); co-exists for preview panel specificity |
| `hooks/usePreviewUrl.ts` | Thin re-export wrapper: delegates to `useDevserverUrlFromLogs` | `usePreviewUrl`, `PreviewUrlInfo`, `detectPreviewUrl` | Imported by `containers/PreviewBrowserContainer.tsx` | Thin wrapper; minor redundancy with underlying hook |
| `hooks/useWorkspaces.ts` | WebSocket-streaming workspace list (active + archived) with REST summary overlay; exposes `workspaceSummaryKeys` query key factory | `useWorkspaces`, `SidebarWorkspace`, `Workspace` (alias), `workspaceKeys`, `workspaceSummaryKeys`, `UseWorkspacesResult` | Imported by `contexts/WorkspaceContext.tsx`, `contexts/CreateModeContext.tsx`, `hooks/useCreateWorkspace.ts`, `hooks/useAttemptCreation.ts`, `hooks/useDevServer.ts`, `hooks/useTaskMutations.ts`, `ui-new/containers/*`, `dialogs/RenameWorkspaceDialog.tsx` | Core data hook; `workspaceKeys` exported but unused externally |
| `scope/NewDesignScope.tsx` | Root provider boundary: mounts all context providers (`WorkspaceProvider`, `ExecutionProcessesProvider`, `ActionsProvider`, `NiceModal.Provider`, `PortalContainerContext`, `ToastProvider`) + fires PostHog `ui_new_accessed` once | `NewDesignScope` | Imported by `App.tsx` (4 route wrappers) | Critical infrastructure — removing breaks all new-design routes |
| `types/fileTree.ts` | Shared TypeScript types for file-tree display | `TreeNode`, `TreeNodeType` | Imported by `utils/fileTreeUtils.ts`, `views/FileTree.tsx`, `views/FileTreeNode.tsx` | Fully active |
| `utils/workflowStatus.ts` | Display config + resolver functions for workflow and terminal statuses | `getWorkflowStatusMeta`, `getTerminalStatusMeta`, `StatusMeta`, `StatusTone` | Both functions **have zero callers** in the frontend src. Contains inline comments referencing G13-005, G14-007, G14-009 lint/audit gate items. | HIGH PRIORITY candidate: both public exports appear unused; `idle`/`queued` are client-only states noted as non-canonical. |

---

## Candidates

| Path | Kind | Evidence | Disposition | Confidence | Blast Radius |
|------|------|----------|-------------|------------|--------------|
| `actions/index.ts` — `Actions.OpenInIDE` (lines 640–665) | dubious-feature | Labelled G1 deletion candidate in task scope; uses `ide-icon` special type which requires special handling in `ContextBar.tsx` L163 and `CommandBar.tsx` L22; feature calls `attemptsApi.openEditor`. The old `Navbar.tsx` and `DiffCard.tsx` have their own parallel `handleOpenInIDE` implementations. | investigate | medium | Removing breaks ContextBar primary group and CommandBar ide-icon rendering paths |
| `actions/index.ts` — `Actions.OpenInOldUI` (lines 579–608) | legacy | Navigates from new UI back to classic UI routes (`/projects/:id/tasks/:id`). Referenced in `NavbarActionGroups.left` and `pages.ts` root page. Represents a legacy escape hatch that presupposes both UIs coexist. | investigate | medium | Removing clears one navbar-left slot and the root command bar entry |
| `actions/useActionVisibility.ts` — `filterVisibleActionItems` (lines 137–164) | dead | Exported but zero callers in production source (grep confirmed). Visibility filtering in `NavbarContainer` and `ContextBar.tsx` is done inline using `isActionVisible`. | delete | high | Zero callers; safe to remove the export; underlying helper functions it calls are still used individually |
| `hooks/useWorkspaces.ts` — `workspaceKeys` (line 78–80) | dead | Exported const `workspaceKeys = { all: ['workspaces'] }` has zero external callers (grep confirmed). `workspaceSummaryKeys` is widely used; `workspaceKeys` is not. | delete | high | Zero callers; removing export only |
| `utils/workflowStatus.ts` — `getWorkflowStatusMeta` + `getTerminalStatusMeta` (lines 239–251) | dead | Both exported functions have zero callers in the entire `frontend/src` tree (grep confirmed). The file has inline audit comments (G13-005, G14-007, G14-009) but those are documentation notes, not usages. | delete | high | Removing the whole file or just the two exports is safe; `TERMINAL_STATUS_CONFIG` and `WORKFLOW_STATUS_CONFIG` are private to the file |
| `hooks/usePreviewUrl.ts` | redundant | Pure re-export wrapper delegating to `useDevserverUrlFromLogs` / `detectDevserverUrl`. Adds no logic; callers could import the underlying hooks directly. | refactor | low | Only 1 caller (`PreviewBrowserContainer.tsx`); low blast radius but worth consolidating |

---

## Invisible Features

| Name | What it does | Seems used | User visible |
|------|-------------|-----------|--------------|
| PostHog survey trigger | `Actions.Feedback.execute` calls `posthog.displaySurvey('019bb6e8...')` — hardcoded survey ID | Yes, wired to Feedback action in navbar + command bar | No (background analytics) |
| PostHog `ui_new_accessed` event | `NewDesignScope` fires a one-shot event when new UI is first mounted | Yes | No |
| Dev server auto-open preview | `Actions.ToggleDevServer.execute` calls `setRightMainPanelMode(PREVIEW)` when starting | Yes | Partially (side-effect, not labeled) |
| Command bar repo selector state | `useCommandBarState` has `selectingRepo` status for multi-repo git actions — shows repo picker before executing git ops | Yes (multi-repo paths) | Yes |
| `workspaceKeys` query key factory | Exported from `useWorkspaces.ts` but currently unused — reserved for potential future query invalidation patterns | No | No |
| `idle` / `queued` in `WORKFLOW_STATUS_CONFIG` | Noted in inline comment (G14-007) as "client-only display states not part of backend WorkflowStatus enum" | No callers for the containing functions | No |
