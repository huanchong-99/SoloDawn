# Census: rs-server-routes-rest-B
## Scope: crates/server/src/routes/*.rs — alphabetical second half (organizations.rs through ws_origin.rs)

| File | Purpose | Public Surface | Key Relations | Notes |
|------|---------|----------------|---------------|-------|
| organizations.rs | Stub org/invitation CRUD — all endpoints return 501-style BadRequest | `router()` → 15 routes (GET/POST/PATCH/DELETE on `/organizations/**`, `/invitations/**`) | Mounted in `mod.rs` via `organizations::router()`. Imports DTOs from `utils::api::organizations`. No frontend callers found. | Entirely stubbed; header comment says "All routes return 501 Not Implemented". |
| planning_drafts.rs | Planning draft lifecycle (create→gather→spec_ready→confirmed→materialized) + Feishu sync + audit doc upload | `planning_draft_routes()`, DTOs: `DraftResponse`, `MessageResponse`, `MaterializeResponse`, `ConfirmDraftRequest`, `CreateDraftRequest`, `UpdateSpecRequest`, `SendMessageRequest` | Calls `crate::routes::workflows::auto_prepare_and_start` on materialize. Uses `services::orchestrator::{generate_audit_plan, create_claude_code_native_client, create_llm_client}`. Frontend: 6 files in `frontend/src` import planning-draft API. | Implements AuditPlan System B (confirm→materialize flow). Active feature. |
| projects.rs | Project CRUD, WS stream, file search, editor-open, remote project stubs, repo management | `router()` (takes `&DeploymentImpl`), 18 `pub async fn` handlers, DTO types: `OpenEditorRequest`, `OpenEditorResponse`, `ResolveProjectByPathRequest/Response`, `LinkToExistingRequest`, `CreateRemoteProjectRequest` | Depends on `services::project`, `services::file_search`. `open_project_in_editor` calls `config.editor.with_override(…).open_file_with_hint()`. Remote-link endpoints stub out. Frontend calls via `api.ts` openEditor. | Contains "open in external IDE/editor" feature (G1). Duplicate `resolve_repo_file_path_for_editor` / `resolve_editor_target_file_hint` also in `repo.rs`. |
| provider_health.rs | Circuit-breaker metrics per workflow; reset endpoint | `get_provider_status`, `reset_provider`, `provider_health_routes()` | Calls `deployment.orchestrator_runtime().get_provider_status/reset_provider`. Fallback: `ModelConfig::find_all`. | Live metrics only available when orchestrator is running. |
| quality.rs | Quality gate REST: list runs, get run detail, get issues, latest terminal run | `list_quality_runs`, `get_quality_run`, `get_quality_issues`, `get_terminal_latest_quality`; router fns: `quality_workflow_routes()`, `quality_routes()`, `quality_terminal_routes()`; DTOs: `QualityRunSummary`, `QualityRunDetail` | Reads `db::models::QualityRun`, `QualityIssueRecord`. Registered at `/workflows/{id}/quality/runs`, `/quality/runs/…`, `/terminals/{id}/quality/latest`. | Quality Gate System A read surface. Three-layer split into three sub-routers. |
| repo.rs | Repo CRUD + register/init, branch listing, batch lookup, file search, editor-open | `router()`, pub async fns: `register_repo`, `init_repo`, `get_repos`, `get_repo`, `update_repo`, `get_repo_branches`, `get_repos_batch`, `open_repo_in_editor`, `search_repo` | Uses `deployment.repo()`, `deployment.git()`, `services::file_search`. `open_repo_in_editor` calls `config.editor.with_override(…).open_file_with_hint()`. Imports `OpenEditorRequest/Response` from `projects.rs`. | Contains "open in external IDE/editor" feature (G1). Local `resolve_repo_file_path_for_editor` + `resolve_editor_target_file_hint` duplicate equivalent fns in `projects.rs`. |
| scratch.rs | CRUD + WS stream for scratch pad items (keyed by `scratch_type + id`) | `router()`, pub fns: `list_scratch`, `get_scratch`, `create_scratch`, `update_scratch`, `delete_scratch`, `stream_scratch_ws` | Uses `deployment.events().stream_scratch_raw(…)` and `queued_message_service().has_queued(id)`. Validates scratch payload type vs URL type. | Scratch is an active feature (note: queued-message guard). |
| setup.rs | One-time setup flow: check completion status, mark complete | `router()` → 2 routes: `GET /setup/status`, `POST /setup/complete` | Reads `SystemSetting`, `Project::count`, `config().workflow_model_library`. Idempotent guard on mark_complete. | Small but active — frontend wizard depends on this. |
| shared_tasks_types.rs | Type-only definitions for remote shared tasks (disabled feature) | Types exported via TS: `UserData`, `SharedTask`, `SharedTaskResponse`, `SharedTaskDetails`, `AssigneesQuery`, `AssignSharedTaskRequest` | Only referenced in `bin/generate_types.rs` for TS type generation. No runtime route handlers. Not imported by any route handler file. | Remote sharing stub. No HTTP routes, only TS type exports. |
| slash_commands.rs | CRUD for slash command presets (user-defined, system presets protected) | `slash_commands_routes()`, pub fns: `list_command_presets`, `get_command_preset`, `create_command_preset`, `update_command_preset`, `delete_command_preset` | Raw `sqlx` queries (no model abstraction). Mounted under `/workflows/presets/commands`. | Inline SQL (no ORM model). System presets protected from modification/deletion. |
| subscription_hub.rs | Per-workflow broadcast hub with pending-event cache (TTL + size cap) | `SubscriptionHub` struct, `SharedSubscriptionHub` type alias, methods: `subscribe`, `publish`, `publish_lagged_to_active`, `subscriber_count`, `has_subscribers`, `cleanup_if_idle`, `channel_count` | Used by `EventBridge` (event_bridge.rs), `workflow_ws.rs`, `main.rs`, `self_test/runner.rs`. | Core WS infrastructure. Active, well-tested (10 unit tests). |
| system_settings.rs | Read/write system settings key-value store; Feishu enable toggle | `router()` → `GET/PUT /system-settings`. Admin gate via `check_admin`. | Uses `SystemSetting::find_all/set`. Admin mutation gated on `SOLODAWN_ADMIN_TOKEN`. | Long-term G24 note: replace with RBAC on `RequestContext`. |
| tags.rs | Tag CRUD (list, create, update, delete) | `router()`, pub fns: `get_tags`, `create_tag`, `update_tag`, `delete_tag` | Uses `Tag::find_all/create/update/delete`. Middleware: `load_tag_middleware`. | Active. |
| task_attempts.rs | Workspace (task attempt) full lifecycle: create, run setup/cleanup/dev-server, merge, push, rebase, conflict abort, editor-open, search, PR, branch ops | `router()` (takes `&DeploymentImpl`). Many pub async fns. Sub-modules: `codex_setup`, `cursor_setup`, `gh_cli_setup`, `images`, `pr`, `util`, `workspace_summary`. DTOs: `BranchStatus`, `RepoBranchStatus`, `RenameBranchError`, etc. | Calls `deployment.container()`, `deployment.git()`. `open_task_attempt_in_editor` calls `config.editor.with_override(…).open_file_with_hint()`. `get_planning_messages` traces Workspace→Task→WorkflowTask→PlanningDraft. | Most complex route file (2168 lines). Contains "open in external IDE/editor" (G1) and planning-message bridge endpoint. |
| tasks.rs | Task CRUD, WS stream, create-and-start, auto-create-workspace, queued-message | `router()`, pub fns: `get_tasks`, `get_task`, `create_task`, `create_task_and_start`, `update_task`, `delete_task`, `stream_tasks_ws`, plus queued-message handlers | Auth-gated `get_tasks` when `SOLODAWN_REQUIRE_AUTH=1`. Uses `Task`, `Workspace`, `WorkspaceRepo`, `Repo`, `TaskImage`. | Active. Auth-opt-in guard. |
| terminal_ws.rs | WebSocket handler for PTY terminal I/O with reconnect/resume (seq-based) | `terminal_ws_routes()`, `validate_terminal_id()`, `WsMessage` enum. `TerminalIO` struct (dead, BACKLOG-002). | Uses `deployment.process_manager()`, `deployment.prompt_watcher()`. Origin validation via `ws_origin::validate_ws_origin`. SEC-003/SEC-017 applied. | BACKLOG-002: `TerminalIO` and `from_process_handle` are `#[allow(dead_code)]` stubs for future gRPC migration. |
| terminals.rs | Terminal start/stop/get/list/log endpoints for workflow terminals | `router()`, pub fns: `start_terminal`, `stop_terminal`, `get_terminal`, `list_terminals`, `get_terminal_logs`, etc. `TerminalSpawnConfig` struct, `spawn_command_to_runner_config` fn (BACKLOG-002 dead). | Uses `CCSwitchService`, `TerminalBridge`, `ProcessManager`, `OrchestratorRuntime`. `start_terminal` called from `workflows.rs`. | BACKLOG-002: `TerminalSpawnConfig` + `spawn_command_to_runner_config` are `#[allow(dead_code)]` stubs. |
| workflow_events.rs | WsEvent type + BusMessage→WsEvent conversion; WsEventType enum | `WsEvent`, `WsEventType`, factory methods `new/heartbeat/lagged/error/try_from_bus_message` | Used by `subscription_hub.rs`, `workflow_ws.rs`, `event_bridge.rs`. TS types emitted via `generate_types.rs`. | `OrchestratorAwakened` and `OrchestratorDecision` variants are `#[allow(dead_code)]` — reserved for stable WS contract. |
| workflow_ws.rs | Workflow event WebSocket: subscribe, heartbeat, forward prompt responses | `workflow_ws_routes()` → `GET /workflow/{id}/events` | Uses `SharedSubscriptionHub`, `OrchestratorRuntime.submit_user_prompt_response`. SEC-003/SEC-017 applied. | Active. Handles bidirectional prompt-response flow. |
| workflows.rs | Workflow CRUD, prepare/start/stop/pause/resume/cancel, recovery, terminal management, git-watcher | `router()`, `auto_prepare_and_start()` (pub). Many internal handlers. | Large file. Calls `start_terminal` from `terminals.rs`. Called by `planning_drafts.rs::materialize_draft`. `auto_prepare_and_start` is the public entrypoint for auto-start on materialize. | In-flight relevance: materialized workflows are auto-started via this. Contains audit_plan propagation. |
| workflows_dto.rs | API DTOs for workflow responses with explicit camelCase serialization | `WorkflowDetailDto`, `WorkflowTaskDto`, `TerminalDto`, `WorkflowCommandDto`, `SlashCommandPresetDto`, `WorkflowListItemDto` + conversion impls + TS export | Used by `workflows.rs` handlers. TS exported. `custom_api_key` explicitly excluded (`#[serde(skip_serializing)]`). | TODO comment on `merge_terminal_cli_id` Option→String migration pending G01-005/G17-006. |
| ws_origin.rs | Shared WS Origin validation (CORS allowlist check) | `validate_ws_origin(headers)` | Imported by `terminal_ws.rs` and `workflow_ws.rs`. Reads `SOLODAWN_CORS_ORIGINS` env var. | Dev mode: allows all origins with a warning. |
