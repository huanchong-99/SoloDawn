# Census: rs-services-container-merge

Unit: `rs-services-container-merge`
Branch: `refactor/streamline-quality-gates`
Date: 2026-06-14

## Module Map

| File | Purpose | Public Surface | Relations | Notes |
|------|---------|---------------|-----------|-------|
| `crates/services/src/services/container.rs` | Core trait `ContainerService` defining the lifecycle contract for workspace execution containers (create, run, stop, finalize, log-stream, git-branch). | **Trait**: `ContainerService` (async trait with default impls). **Type**: `ContainerRef = String`. **Error**: `ContainerError`. **Key default methods**: `has_running_processes`, `should_finalize`, `finalize_task`, `cleanup_orphan_executions`, `backfill_before_head_commits`, `backfill_repo_names`, `cleanup_actions_for_repos`, `setup_actions_for_repos`, `setup_action_for_repo`, `build_sequential_setup_chain`, `try_stop`, `get_msg_store_by_id`, `git_branch_from_workspace`, `stream_raw_logs`, `stream_normalized_logs`, `spawn_stream_raw_logs_to_db`, `start_workspace`, `start_execution`, `try_start_next_action`. **Abstract methods** (no default): `create`, `kill_all_running_processes`, `delete`, `ensure_container_exists`, `is_container_clean`, `start_execution_inner`, `stop_execution`, `try_commit_changes`, `copy_project_files`, `stream_diff`, `git_branch_prefix`. | Implemented only by `LocalContainerService` (`crates/local-deployment/src/container.rs`). Trait methods called by: `crates/local-deployment/src/container.rs` (lifecycle), `crates/server/src/routes/task_attempts.rs` (HTTP handlers), `crates/server/src/routes/sessions/mod.rs`, `crates/server/src/routes/execution_processes.rs` (WebSocket log streams), `crates/server/src/main.rs` (startup cleanup). Uses `quality::engine::QualityEngine` for inline post-execution quality gate. | TODO [W2-36-10]: detached `tokio::spawn` in `finalize_task` is fire-and-forget; handle not tracked. NOTE(E27-08): `working_dir` in script actions set to `repo.name` (relative), not full path — tracked bug. `has_running_processes` has 0 external callers outside its own file (only referenced in trait def). |
| `crates/services/src/services/merge_coordinator.rs` | Coordinates squash-merge of completed task branches into the base branch for the Orchestrator workflow system. Provides per-workflow mutex locking to prevent concurrent auto-merge + manual-merge races. | **Struct**: `MergeCoordinator { db, message_bus, git_service }`. **Pub fns**: `MergeCoordinator::new`, `MergeCoordinator::merge_task_branch`, `MergeCoordinator::resolve_and_complete_merge`. **Pub fn (free)**: `acquire_workflow_merge_lock`. **Private**: `handle_merge_conflict`, `broadcast_merge_success`, `broadcast_merge_failure`. | `MergeCoordinator::new` + `merge_task_branch` called from `crates/services/src/services/orchestrator/agent.rs` (auto-merge path after all tasks complete). `acquire_workflow_merge_lock` called from `crates/server/src/routes/workflows.rs` (manual merge REST endpoint, G06-002). `resolve_and_complete_merge` called only within `merge_coordinator.rs` itself (no external caller found). | `resolve_and_complete_merge` is PUBLIC with 0 external callers — only referenced internally (self-call in no external site). Placeholder unit test in file body; real tests in `crates/services/tests/merge_coordinator_test.rs` (stub-only, no real merge exercised). `broadcast_merge_success`/`broadcast_merge_failure`/`handle_merge_conflict` are private impl details. |
| `crates/services/src/services/git_watcher.rs` | Polls a git repository for new commits; parses `---METADATA---` blocks embedded in commit messages; publishes `TerminalCompleted` or `GitEvent` messages to the orchestrator message bus. Used by the Orchestrator workflow runtime to detect when AI terminals commit their results. | **Struct**: `GitWatcher { config, message_bus, workflow_id, last_commit_hash, is_running }`. **Pub fns**: `GitWatcher::new`, `set_workflow_id`, `workflow_id`, `is_running`, `seed_last_seen_commit`, `watch`, `stop`. **Struct**: `GitWatcherConfig`. **Struct**: `CommitMetadata` (with `parse`, `to_orchestrator_metadata`). **Struct**: `ParsedCommit`. **Pub fn (free)**: `parse_commit_metadata`. | Instantiated and managed by `crates/services/src/services/orchestrator/runtime.rs` (`GitWatcherHandle` pattern, per-workflow). `CommitMetadata` / `parse_commit_metadata` also used in `crates/services/src/services/orchestrator/agent.rs`. `seed_last_seen_commit` called from `orchestrator/runtime.rs` on recovery (R8-C2). `db::models::git_event` imported by git_watcher. Re-exported via `crates/services/src/lib.rs`. | `parse_commit_metadata` (free fn) is a thin wrapper over `CommitMetadata::parse` for Result-based callers. Both exist simultaneously — potential redundancy. Good test coverage in-file. R8-C2 cursor pre-seeding is a correctness fix for restart scenarios (invisible feature: prevents permanent task hang after server restart). G10-005, G10-006, G10-011, G10-012 document incremental correctness fixes embedded in the polling logic. |

## Key Issue Notes

### container.rs
- `has_running_processes` (lines 104-123): defined as a default method on the trait; 0 callers found in any external file (only the trait definition itself).
- `working_dir` for script actions uses `repo.name` (relative), documented as a bug under NOTE(E27-08) (lines 461, 502, 538). Three separate methods all share this same defect: `cleanup_actions_for_repos`, `setup_actions_for_repos`, `setup_action_for_repo`.
- `finalize_task` (lines 173-296): fires a detached `tokio::spawn` (TODO [W2-36-10]) for the quality gate — handle discarded, not tracked for shutdown.
- Quality gate integration (lines 246-269): calls `quality::engine::QualityEngine::from_project` + `engine.run` inline — this is the System A quality gate hookup.

### merge_coordinator.rs
- `resolve_and_complete_merge` (lines 228-248): public method, but 0 external callers found. Only self-referenced in the file's own tests (placeholder) and not wired to any REST route or orchestrator path.
- Placeholder test `test_merge_coordinator_creation` (lines 325-328) does nothing and explicitly says "Real tests are in merge_coordinator_test.rs" — but even those tests are stub-only (no actual merge invoked).

### git_watcher.rs
- `parse_commit_metadata` free fn (lines 747-751): thin Result-wrapping wrapper over `CommitMetadata::parse`. Potentially redundant with the method form; both coexist.
- `GitWatcher::is_running` (lines 238-240) and `GitWatcher::workflow_id` (lines 233-235): accessors used internally and in `orchestrator/runtime.rs`.
